namespace = dynamic_missions

# Dynamic Missions: Choose Your Goal
dynamic_missions.1 = {
	type = country_event
	title = dynamic_missions.1.title
	desc = dynamic_missions.1.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = happy
		10 = interior
	}
	
	trigger = {
		is_ai = no
		NOT = { has_variable = active_dynamic_mission }
	}
	
	# Option 1: Focus on Developing a City
	option = {
		name = dynamic_missions.1.option_a
		trigger = { can_start_develop_city_mission = yes }
		set_variable = { name = active_dynamic_mission value = yes }
		trigger_event_non_silently = { id = dynamic_missions.2 }
		custom_tooltip = dynamic_missions.1.option_a.tooltip
	}

	# Option 2: Large Research Project
	option = {
		name = "Start a Large Research Project"
		trigger = { can_start_large_research_project = yes }
		set_variable = { name = active_dynamic_mission value = yes }
		# TODO: Situations cannot be manually activated in EU5
		# activate_situation = large_research_project_situation
		custom_tooltip = "Embark on a 5-year research project to gain a significant technology boost."
	}
	
	# Option 3: Establish a New City
	option = {
		name = dynamic_missions.1.option_d
		trigger = { can_start_establish_new_city_mission = yes }
		set_variable = { name = active_dynamic_mission value = yes }
		trigger_event_non_silently = { id = dynamic_missions.30 }
		custom_tooltip = dynamic_missions.1.option_d.tooltip
	}
	
	# Option 4: Claim a Province
	option = {
		name = dynamic_missions.1.option_e
		trigger = { can_start_claim_province_mission = yes }
		set_variable = { name = active_dynamic_mission value = yes }
		trigger_event_non_silently = { id = dynamic_missions.40 }
		custom_tooltip = dynamic_missions.1.option_e.tooltip
	}
	
	# Option 5: Not now
	option = {
		name = dynamic_missions.1.option_c
		custom_tooltip = dynamic_missions.1.option_c.tooltip
	}
}

# Dynamic Missions: Select Target City
dynamic_missions.2 = {
	type = country_event
	title = dynamic_missions.2.title
	desc = dynamic_missions.2.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = exterior
		10 = city
	}
	
	immediate = {
		every_owned_location = {
			limit = {
				OR = {
					location_rank = location_rank:town
					location_rank = location_rank:city
				}
			}
			add_to_list = eligible_cities
		}
	}
	
		option = {
			name = dynamic_missions.2.option_a
			capital = {
				save_scope_as = target_city
				set_variable = { name = selected_for_development value = yes }
				set_variable = { name = initial_building_count value = num_buildings }
			}
			# Set variable to trigger situation next month
			set_variable = { name = wants_develop_city value = yes }
			custom_tooltip = dynamic_missions.2.option_a.tooltip
		}
	
	option = {
		name = dynamic_missions.2.option_b
		remove_variable = active_dynamic_mission
		custom_tooltip = dynamic_missions.2.option_b.tooltip
	}
}

# Dynamic Missions: City Development Complete
dynamic_missions.3 = {
	type = country_event
	title = dynamic_missions.3.title
	desc = dynamic_missions.3.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = happy
		10 = city
	}
	
	option = {
		name = dynamic_missions.3.option_a
		situation:develop_city_situation = {
			var:target_location = { add_development = 1 }
		}
		remove_variable = active_dynamic_mission
		trigger_event_non_silently = { id = dynamic_missions.1 days = 30 }
		custom_tooltip = dynamic_missions.3.option_a.tooltip
	}
}

# Dynamic Missions: City Development Failed
dynamic_missions.4 = {
	type = country_event
	title = dynamic_missions.4.title
	desc = dynamic_missions.4.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = sad
		10 = city
	}
	
	option = {
		name = dynamic_missions.4.option_a
		remove_variable = active_dynamic_mission
		trigger_event_non_silently = { id = dynamic_missions.1 days = 30 }
		custom_tooltip = dynamic_missions.4.option_a.tooltip
	}
}

# --- Large Research Project Events ---

# Dynamic Missions: Select University to Conscript
dynamic_missions.21 = {
	type = country_event
	title = dynamic_missions.21.title
	desc = dynamic_missions.21.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = interior
		10 = library
	}
	
	# TODO: Implement dynamic location selection
	# For now, use a simple option to conscript first available university
	option = {
		name = "Conscript a university"
		random_owned_location = {
			limit = {
				has_building = building_type:university
				NOT = { has_location_modifier = dm_conscripted_university_debuff }
			}
			add_location_modifier = { modifier = dm_conscripted_university_debuff }
		}
		situation:large_research_project_situation = {
			if = {
				limit = { NOT = { has_variable = conscripted_universities_count } }
				set_variable = { name = conscripted_universities_count value = 0 }
			}
			change_variable = { name = conscripted_universities_count add = 1 }
		}
	}
}

# Dynamic Missions: Appoint Lead Scientist
dynamic_missions.22 = {
	type = country_event
	title = dynamic_missions.22.title
	desc = dynamic_missions.22.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = character
		10 = interior
	}
	
	# TODO: Implement dynamic character selection
	# For now, use a simple option to appoint first available character
	option = {
		name = "Appoint a lead scientist"
		random_character = {
			# TODO: Add proper court character filter
			save_scope_as = scientist
			add_character_modifier = { modifier = dm_lead_scientist_debuff }
		}
		situation:large_research_project_situation = {
			set_variable = { name = lead_scientist_appointed value = yes }
			set_variable = { name = lead_scientist_char value = scope:scientist }
			change_variable = { name = research_progress add = 5 }
		}
	}
}

# Dynamic Missions: Large Research Project Success
dynamic_missions.23 = {
	type = country_event
	title = dynamic_missions.23.title
	desc = dynamic_missions.23.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = happy
		10 = interior
	}
	
	immediate = {
		# add_technology_progress = { category = adm progress = 20 }
	}
	
	option = {
		name = dynamic_missions.23.option_a
		remove_variable = active_dynamic_mission
		trigger_event_non_silently = { id = dynamic_missions.1 days = 30 }
	}
}

# Dynamic Missions: Large Research Project Failure
dynamic_missions.24 = {
	type = country_event
	title = dynamic_missions.24.title
	desc = dynamic_missions.24.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = sad
		10 = interior
	}
	
	option = {
		name = dynamic_missions.24.option_a
		remove_variable = active_dynamic_mission
		trigger_event_non_silently = { id = dynamic_missions.1 days = 30 }
	}
}


# =============================================================================
# ESTABLISH NEW CITY MISSION EVENTS
# =============================================================================

# Dynamic Missions: Select Target Town for New City
dynamic_missions.30 = {
	type = country_event
	title = dynamic_missions.30.title
	desc = dynamic_missions.30.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = exterior
		10 = town
	}
	
	immediate = {
		# Build list of eligible towns
		every_owned_location = {
			limit = {
				location_rank = location_rank:town
				# is_occupied = no
				# is_under_siege = no
			}
			add_to_list = eligible_towns
		}
	}
	
	# Dynamic options for each eligible town
	# Note: In actual implementation, you may need to use a fixed number of options
	# or implement a selection mechanism via variables
	
	# Option 1: Select Capital (if it's a town)
	option = {
		name = dynamic_missions.30.option_capital
		trigger = {
			capital = {
				location_rank = location_rank:town
			}
		}
		
			capital = {
				save_scope_as = target_town
				set_variable = { name = selected_for_new_city value = yes }
			}
			
			# Set variable to trigger situation next month
			set_variable = { name = wants_establish_new_city value = yes }
		
		custom_tooltip = dynamic_missions.30.option_capital.tooltip
	}
	
	# Option 2: Select First Eligible Town
	option = {
		name = dynamic_missions.30.option_first_town
		trigger = {
			any_in_list = {
				list = eligible_towns
				count >= 1
			}
		}
		
			random_in_list = {
				list = eligible_towns
				save_scope_as = target_town
				set_variable = { name = selected_for_new_city value = yes }
			}
			
			# Set variable to trigger situation next month
			set_variable = { name = wants_establish_new_city value = yes }
		
		custom_tooltip = dynamic_missions.30.option_first_town.tooltip
	}
	
	# Option 3: Cancel
	option = {
		name = dynamic_missions.30.option_cancel
		remove_variable = active_dynamic_mission
		custom_tooltip = dynamic_missions.30.option_cancel.tooltip
	}
}

# Dynamic Missions: Select Character for Regional Governor
dynamic_missions.31 = {
	type = country_event
	title = dynamic_missions.31.title
	desc = dynamic_missions.31.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = character
		10 = interior
	}
	
	# TODO: Implement dynamic character selection
	# For now, use a simple option to appoint first available character
	option = {
		name = "Appoint a regional governor"
		random_character = {
			# TODO: Add proper court character filter
			save_scope_as = governor
			add_character_modifier = {
				modifier = dm_regional_governor_debuff
				years = -1
			}
		}
		
		situation:establish_new_city_situation = {
			set_variable = { name = governor_character value = scope:governor }
			var:target_location = {
				add_location_modifier = {
					modifier = dm_governor_control_bonus
					years = -1
				}
			}
		}
		
		add_legitimacy = -5
		custom_tooltip = dynamic_missions.31.effect.tooltip
	}
	
	# Option: Cancel
	option = {
		name = dynamic_missions.31.option_cancel
		# Remove the "used" flag so player can try again
		remove_country_modifier = dm_appoint_governor_used
		custom_tooltip = dynamic_missions.31.option_cancel.tooltip
	}
}

# Dynamic Missions: New City Established - Success
dynamic_missions.33 = {
	type = country_event
	title = dynamic_missions.33.title
	desc = dynamic_missions.33.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = happy
		10 = city
	}
	
	option = {
		name = dynamic_missions.33.option_a
		
		# Apply permanent reward modifier
		situation:establish_new_city_situation = {
			var:target_location = {
				add_location_modifier = {
					modifier = dm_country_pearl
					years = -1  # Permanent
				}
			}
		}
		
		# Set completion flag to prevent re-triggering
		set_variable = { name = has_completed_establish_city_mission value = yes }
		
		# Remove active mission variable
		remove_variable = active_dynamic_mission
		
		# Trigger next mission selection after 30 days
		trigger_event_non_silently = { id = dynamic_missions.1 days = 30 }
		
		custom_tooltip = dynamic_missions.33.option_a.tooltip
	}
}

# Dynamic Missions: City Development Failed
dynamic_missions.34 = {
	type = country_event
	title = dynamic_missions.34.title
	desc = dynamic_missions.34.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = sad
		10 = town
	}
	
	option = {
		name = dynamic_missions.34.option_a
		
		# Remove active mission variable (but don't set completion flag)
		remove_variable = active_dynamic_mission
		
		# Trigger next mission selection after 30 days
		trigger_event_non_silently = { id = dynamic_missions.1 days = 30 }
		
		custom_tooltip = dynamic_missions.34.option_a.tooltip
	}
}


# =============================================================================
# NOTIFICATION EVENTS FOR DEVELOP CITY MISSION
# =============================================================================

# Subject Stole Progress - Notification to Overlord
dynamic_missions.10 = {
	type = country_event
	title = dynamic_missions.10.title
	desc = dynamic_missions.10.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = sad
		10 = interior
	}
	
	option = {
		name = dynamic_missions.10.option_a
		# No mechanical effect, just notification
		custom_tooltip = dynamic_missions.10.option_a.tooltip
	}
}

# Subject Contributed to Development - Notification to Overlord
dynamic_missions.11 = {
	type = country_event
	title = dynamic_missions.11.title
	desc = dynamic_missions.11.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = happy
		10 = interior
	}
	
	option = {
		name = dynamic_missions.11.option_a
		# No mechanical effect, just notification
		custom_tooltip = dynamic_missions.11.option_a.tooltip
	}
}

# =============================================================================
# AI EVALUATION EVENT FOR DEVELOP CITY MISSION
# =============================================================================

# AI Evaluation - Subjects decide whether to help or hinder
dynamic_missions.100 = {
	type = country_event
	title = dynamic_missions.100.title
	desc = dynamic_missions.100.desc
	fire_only_once = no
	hidden = yes  # This is a silent event for AI decision-making
	
	trigger = {
		is_ai = yes
		is_subject = yes
		overlord = {
			# has_active_situation = develop_city_situation
		}
	}
	
	# AI evaluates whether to use available actions
	immediate = {
		# Check if disloyal and can steal progress
		if = {
			limit = {
				subject_loyalty < 50
				NOT = { has_country_modifier = dm_steal_progress_cooldown }
				opinion = {
					target = overlord
					value < 0
				}
			}
			# 30% chance to steal if conditions are met
			random = {
				chance = 30
				# Execute the steal progress action
				# Note: This would normally trigger the generic action
				# For now, we'll just set a flag for future implementation
			}
		}
		
		# Check if loyal and can contribute
		if = {
			limit = {
				subject_loyalty >= 50
				NOT = { has_country_modifier = dm_contribute_development_cooldown }
				opinion = {
					target = overlord
					value >= 50
				}
			}
			# 20% chance to contribute if conditions are met
			random = {
				chance = 20
				# Execute the contribute action
				# Note: This would normally trigger the generic action
			}
		}
	}
	
	option = {
		name = "OK"
		# Silent event, no player interaction
	}
}


# =============================================================================
# CLAIM PROVINCE MISSION EVENTS (IDs 40-52)
# =============================================================================

# Dynamic Missions: Start Claim Province Mission
dynamic_missions.40 = {
	type = country_event
	title = dynamic_missions.40.title
	desc = dynamic_missions.40.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = exterior
		10 = military
	}
	
	# Option: Select a province to claim
	option = {
		name = dynamic_missions.40.option_a
		trigger_event_non_silently = { id = dynamic_missions.41 }
		custom_tooltip = dynamic_missions.40.option_a.tooltip
	}
	
	# Option: Cancel
	option = {
		name = dynamic_missions.40.option_b
		remove_variable = active_dynamic_mission
		custom_tooltip = dynamic_missions.40.option_b.tooltip
	}
}

# Dynamic Missions: Select Province to Claim
dynamic_missions.41 = {
	type = country_event
	title = dynamic_missions.41.title
	desc = dynamic_missions.41.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = map
		10 = military
	}
	
	immediate = {
		# Build list of eligible neighboring provinces
		every_owned_location = {
			# every_neighboring_location = {
				limit = {
					owner = {
						NOT = { is_subject_of = root }
						NOT = { this = root }
						exists = yes
					}
				}
				province = {
					limit = {
						NOT = { is_in_list = eligible_provinces }
					}
					add_to_list = eligible_provinces
				}
			}
		}
	}
	
	# Dynamic options for each eligible province
	# Note: In actual implementation, would need multiple option blocks
	# For now, showing the pattern for the first province
	option = {
		name = dynamic_missions.41.option_province
		trigger = {
			any_in_list = {
				list = eligible_provinces
				count >= 1
			}
		}
		
		# Get first eligible province
		random_in_list = {
			list = eligible_provinces
			limit = { always = yes }
			save_scope_as = selected_province
			owner = { save_scope_as = target_owner }
		}
		
		# Save target information to country variables
		scope:selected_province = {
			save_scope_as = target_province
			set_variable = { name = claimed_by_player value = yes }
			owner = { save_scope_as = target_owner }
		}
		set_variable = { name = claim_target_province value = scope:target_province }
		set_variable = { name = claim_target_country value = scope:target_owner }
		
		# Set variable to trigger situation next month
		set_variable = { name = wants_claim_province value = yes }
		
		custom_tooltip = dynamic_missions.41.option_province.tooltip
	}
	
	# Option: Cancel
	option = {
		name = dynamic_missions.41.option_cancel
		remove_variable = active_dynamic_mission
		custom_tooltip = dynamic_missions.41.option_cancel.tooltip
	}
}

# Dynamic Missions: Rival Support Request (To Potential Supporter)
dynamic_missions.42 = {
	type = country_event
	title = dynamic_missions.42.title
	desc = dynamic_missions.42.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = diplomacy
		10 = military
	}
	
	immediate = {
		# Calculate willingness to support
		dm_calculate_rival_support_willingness = yes
	}
	
	# Option: Accept support request
	option = {
		name = dynamic_missions.42.option_accept
		trigger = {
			# Show acceptance chance based on willingness
			var:support_willingness >= 0
		}
		
		# Add this country to player supporters list
		situation:claim_province_situation = {
			add_to_list = player_supporters_list
			change_variable = { name = player_supporters_count add = 1 }
		}
		
		# Notify player
		# TODO: Fix scope - situation.owner not accessible
		# situation:claim_province_situation.owner = {
			trigger_event_non_silently = { id = dynamic_missions.43 days = 1 }
		}
		
		custom_tooltip = dynamic_missions.42.option_accept.tooltip
		
		# Show calculated willingness
		custom_tooltip = {
			text = dynamic_missions.42.willingness_tooltip
			# Willingness: [var:support_willingness]
		}
	}
	
	# Option: Decline support request
	option = {
		name = dynamic_missions.42.option_decline
		
		custom_tooltip = dynamic_missions.42.option_decline.tooltip
	}
}

# Dynamic Missions: Rival Accepted Support (Notification to Player)
dynamic_missions.43 = {
	type = country_event
	title = dynamic_missions.43.title
	desc = dynamic_missions.43.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = happy
		10 = diplomacy
	}
	
	option = {
		name = dynamic_missions.43.option_a
		custom_tooltip = dynamic_missions.43.option_a.tooltip
	}
}

# Dynamic Missions: Target Seeks Rival Support (Notification to Player)
dynamic_missions.44 = {
	type = country_event
	title = dynamic_missions.44.title
	desc = dynamic_missions.44.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = diplomacy
		10 = worried
	}
	
	option = {
		name = dynamic_missions.44.option_a
		custom_tooltip = dynamic_missions.44.option_a.tooltip
	}
}

# Dynamic Missions: Ultimatum - Player Decision
dynamic_missions.45 = {
	type = country_event
	title = dynamic_missions.45.title
	desc = dynamic_missions.45.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = military
		10 = serious
	}
	
	immediate = {
		# Calculate coalition strengths for display
		situation:claim_province_situation = {
			dm_calculate_player_coalition_strength = yes
			dm_calculate_target_coalition_strength = yes
		}
	}
	
	# Option: Declare War
	option = {
		name = dynamic_missions.45.option_war
		
		# Trigger target's decision event
		situation:claim_province_situation = {
			var:target_country = {
				trigger_event_non_silently = { id = dynamic_missions.46 }
			}
		}
		
		custom_tooltip = dynamic_missions.45.option_war.tooltip
		custom_tooltip = {
			text = dynamic_missions.45.supporters_tooltip
			# Your supporters: [player_supporters_count]
			# Their coalition: [target + allies + guarantors]
		}
	}
	
	# Option: Back Down
	option = {
		name = dynamic_missions.45.option_backdown
		
		# Set flag and end situation
		situation:claim_province_situation = {
			set_variable = { name = player_backed_down value = yes }
		}
		
		# Apply penalties
		add_prestige = -10
		situation:claim_province_situation = {
			var:target_country = {
				add_prestige = 10
			}
		}
		
		custom_tooltip = dynamic_missions.45.option_backdown.tooltip
	}
}

# Dynamic Missions: Ultimatum - Target Decision
dynamic_missions.46 = {
	type = country_event
	title = dynamic_missions.46.title
	desc = dynamic_missions.46.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = military
		10 = serious
	}
	
	immediate = {
		# Check if AI should surrender
		situation:claim_province_situation = {
			dm_check_ai_surrender_condition = yes
		}
	}
	
	# Option: Fight (AI almost always chooses this unless 5x weaker)
	option = {
		name = dynamic_missions.46.option_fight
		trigger = {
			OR = {
				is_ai = no
				situation:claim_province_situation = {
					has_variable = ai_will_fight
				}
			}
		}
		
		# Execute war declaration
		# TODO: Fix scope - situation.owner not accessible
		# situation:claim_province_situation.owner = {
			dm_execute_claim_war_declaration = yes
		}
		
		custom_tooltip = dynamic_missions.46.option_fight.tooltip
		
		ai_chance = {
			base = 100
		}
	}
	
	# Option: Surrender (AI only if 5x weaker)
	option = {
		name = dynamic_missions.46.option_surrender
		
		# Transfer province to player
		# TODO: Fix scope - situation.owner not accessible
		# situation:claim_province_situation.owner = {
			dm_transfer_claimed_province = yes
		}
		
		# Apply prestige changes
		add_prestige = -10
		# TODO: Fix scope - situation.owner not accessible
		# situation:claim_province_situation.owner = {
			add_prestige = 10
		}
		
		# Set flag and end situation
		situation:claim_province_situation = {
			set_variable = { name = target_surrendered value = yes }
		}
		
		custom_tooltip = dynamic_missions.46.option_surrender.tooltip
		
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				NOT = {
					situation:claim_province_situation = {
						has_variable = ai_will_surrender
					}
				}
			}
		}
	}
}

# Dynamic Missions: War Declared (Notification)
dynamic_missions.47 = {
	type = country_event
	title = dynamic_missions.47.title
	desc = dynamic_missions.47.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = military
		10 = battle
	}
	
	option = {
		name = dynamic_missions.47.option_a
		custom_tooltip = dynamic_missions.47.option_a.tooltip
	}
}

# Dynamic Missions: Reconciliation Achieved
dynamic_missions.48 = {
	type = country_event
	title = dynamic_missions.48.title
	desc = dynamic_missions.48.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = happy
		10 = diplomacy
	}
	
	option = {
		name = dynamic_missions.48.option_a
		
		# Both countries gain opinion
		add_opinion = {
			who = situation:claim_province_situation.var:target_country
			value = 20
		}
		
		situation:claim_province_situation = {
			var:target_country = {
				# TODO: Fix scope - situation.owner not accessible from here
				# add_opinion = {
				#	who = situation:claim_province_situation.owner
				#	value = 20
				# }
			}
		}
		
		custom_tooltip = dynamic_missions.48.option_a.tooltip
	}
}

# Dynamic Missions: Claim Province Success
dynamic_missions.49 = {
	type = country_event
	title = dynamic_missions.49.title
	desc = dynamic_missions.49.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = happy
		10 = military
	}
	
	option = {
		name = dynamic_missions.49.option_a
		
		# Apply prestige rewards
		add_prestige = 10
		situation:claim_province_situation = {
			var:target_country = {
				add_prestige = -10
			}
		}
		
		# Clear active mission flag
		remove_variable = active_dynamic_mission
		
		# Trigger next mission selection after 30 days
		trigger_event_non_silently = { id = dynamic_missions.1 days = 30 }
		
		custom_tooltip = dynamic_missions.49.option_a.tooltip
	}
}

# Dynamic Missions: Player Backed Down
dynamic_missions.50 = {
	type = country_event
	title = dynamic_missions.50.title
	desc = dynamic_missions.50.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = sad
		10 = diplomacy
	}
	
	option = {
		name = dynamic_missions.50.option_a
		
		# Prestige already applied in decision
		# Clear active mission flag
		remove_variable = active_dynamic_mission
		
		# Trigger next mission selection after 30 days
		trigger_event_non_silently = { id = dynamic_missions.1 days = 30 }
		
		custom_tooltip = dynamic_missions.50.option_a.tooltip
	}
}

# Dynamic Missions: Target Surrendered
dynamic_missions.51 = {
	type = country_event
	title = dynamic_missions.51.title
	desc = dynamic_missions.51.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = happy
		10 = military
	}
	
	option = {
		name = dynamic_missions.51.option_a
		
		# Prestige already applied in target's decision
		# Clear active mission flag
		remove_variable = active_dynamic_mission
		
		# Trigger next mission selection after 30 days
		trigger_event_non_silently = { id = dynamic_missions.1 days = 30 }
		
		custom_tooltip = dynamic_missions.51.option_a.tooltip
	}
}

# Dynamic Missions: Reconciliation Completed
dynamic_missions.52 = {
	type = country_event
	title = dynamic_missions.52.title
	desc = dynamic_missions.52.desc
	fire_only_once = no
	
	illustration_tags = {
		10 = happy
		10 = diplomacy
	}
	
	option = {
		name = dynamic_missions.52.option_a
		
		# Opinion bonuses already applied
		# Clear active mission flag
		remove_variable = active_dynamic_mission
		
		# Trigger next mission selection after 30 days
		trigger_event_non_silently = { id = dynamic_missions.1 days = 30 }
		
		custom_tooltip = dynamic_missions.52.option_a.tooltip
	}
}
