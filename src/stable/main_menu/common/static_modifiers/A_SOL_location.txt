## Harsher War Consequences (hwc) group — restored to vanilla values.
## The SOL deltas are now applied by CMM auto_modifiers in SOL_harsher_war_consequences.txt when sol__hwc is ON.

TRY_REPLACE:is_blockaded_by_enemies = {
	game_data = {
		category = location
	}
	local_monthly_prosperity = -0.025              # Vanilla (delta -0.025 handled by sol_hwc_blockade_extras)
	local_maritime_presence = -2.0                 # Vanilla (delta -3.0 handled by sol_hwc_blockade_extras)
	local_max_control = -0.10                      # Vanilla (delta -0.20 handled by sol_hwc_blockade_extras)
	hostile_food_multiplier = -0.75                # Same as vanilla
	local_monthly_development_modifier = -0.20     # Same as vanilla
	local_trade_embark_disembark_cost_modifier = 2.0 # Same as vanilla
	local_construction_speed = -0.10               # Vanilla (delta -0.20 handled by sol_hwc_blockade_extras)
	# local_migration_attraction = -0.5 moved to sol_hwc_blockade_extras
}

# winter_mild: restored to vanilla. HFD delta handled by sol_hfd_winter_mild.
TRY_REPLACE:winter_mild = {
	game_data = {
		category = location
	}
	max_attrition = 2
	local_monthly_food_modifier = -0.25 # Vanilla (delta -0.25 handled by sol_hfd_winter_mild)
}

# winter_normal: restored to vanilla (including food_decay). HFD delta handled by sol_hfd_winter_normal.
TRY_REPLACE:winter_normal = {
	game_data = {
		category = location
	}
	local_army_attrition = 1
	local_navy_attrition = 5
	max_attrition = 2
	local_monthly_food_modifier = -0.50 # Vanilla (delta -0.50 handled by sol_hfd_winter_normal)
	local_food_decay = 0.0025           # Vanilla (neutralized by sol_hfd_winter_normal when hfd ON)
	local_construction_speed = -0.5    # Same as vanilla
}

## Severe winter is same as vanilla ##

# raised_levies: restored to vanilla. HFD delta handled by sol_hfd_raised_levies.
TRY_REPLACE:raised_levies = {
	game_data = {
		category = location
	}
	local_raw_material_output = -0.2 # Vanilla (delta -0.1 handled by sol_hfd_raised_levies)
	local_monthly_food_modifier = -0.2 # Vanilla (delta -0.1 handled by sol_hfd_raised_levies)
}

# under_siege: restored to vanilla (with prosperity_decay). HWC mechanism-change handled by sol_hwc_siege_prosperity.
TRY_REPLACE:under_siege = {
	game_data = {
		category = location
	}
	hostile_food_multiplier = -1.0
	local_monthly_development_modifier = -0.20
	road_building_blocked = yes
	local_prosperity_decay = 0.05               # Vanilla (neutralized by sol_hwc_siege_prosperity when hwc ON)
	constructions_stalled = yes
}

# is_occupied: restored to vanilla. HWC extras handled by sol_hwc_occupied_extras.
TRY_REPLACE:is_occupied = {
	game_data = {
		category = location
	}
	local_monthly_prosperity = -0.025           # Same as vanilla
	local_monthly_control = -0.002              # Vanilla (delta -0.003 handled by sol_hwc_occupied_extras)
	hostile_food_multiplier = -0.5              # Same as vanilla
	local_garrison_growth = -1.0               # Vanilla (delta -0.5 handled by sol_hwc_occupied_extras)
	local_construction_speed = -0.25            # Vanilla (delta -0.50 handled by sol_hwc_occupied_extras)
	# local_monthly_development_modifier -0.1 moved to sol_hwc_occupied_extras
}

# hostile_troops: restored to vanilla (including food effects). HWC extras handled by sol_hwc_hostile_troops_extras.
TRY_REPLACE:hostile_troops = {
	game_data = {
		category = location
	}
	local_monthly_prosperity = -0.025           # Vanilla (delta -0.005 handled by sol_hwc_hostile_troops_extras)
	local_food_decay = 0.0025                   # Vanilla (neutralized by sol_hwc_hostile_troops_extras when hwc ON)
	local_monthly_food_modifier = -0.05         # Vanilla (neutralized by sol_hwc_hostile_troops_extras when hwc ON)
}

###################################
# From Unit Abilities
###################################

# recently_raided_by_horde: restored to vanilla (prosperity only). HWC extras handled by sol_hwc_raided_extras.
TRY_REPLACE:recently_raided_by_horde = {
	game_data = {
		category = location
	}
	local_monthly_prosperity = -0.02 # Same as vanilla (raw_material and food moved to sol_hwc_raided_extras)
}

# recently_razed: restored to vanilla (prosperity only). HWC extras handled by sol_hwc_razed_extras.
TRY_REPLACE:recently_razed = {
	game_data = {
		category = location
	}
	local_monthly_prosperity = -0.02 # Same as vanilla (raw_material and food moved to sol_hwc_razed_extras)
}


### Location Base Values (lb) group — restored to vanilla values.
### The SOL deltas are now applied by CMM auto_modifiers in SOL_location_base.txt when sol__lb is ON.

TRY_REPLACE:location_base_values = {
	game_data = {
		category = location
	}

	local_devastation_recovery = 0.005
	local_monthly_control = 0.0005          # Vanilla (delta +0.0005 handled by sol_lb_base_values_delta)

	max_regiments_trained_at_same_time = 1
	max_constructions_at_same_time = 1

	local_pop_promotion_speed = 0.01
	local_pop_demotion_speed = 0.10

	local_food_capacity = 50

	local_frontage_allowed = 10

	local_migration_attraction = 0.1
	local_garrison_growth = 0.01

	max_attrition = 10
	supply_limit = 15

	hostile_disembark_time_modifier = 0.25

	local_max_rgo_size = 2              # Vanilla (delta -1 handled by sol_lb_base_values_delta)

	local_monthly_prosperity = 0.0025   # Same as vanilla

	local_trade_embark_disembark_cost_modifier = 0.1

	local_clergy_desired_pop_scaled = 0.005
	local_nobles_desired_pop_scaled = 0.0025
}

# inverse_control: SOL version kept (building cost cannot be cleanly CMM-toggled due to control scaling).
# sol__ic is registered in CMM for future use; the effect itself remains always-on with correct IC scaling.
# See SOL_location_base.txt for the ic toggle technical note.
TRY_REPLACE:inverse_control = {
	game_data = {
		category = location
	}

	local_build_buildings_cost = 0.5 # SOL addition (always-on; sol__ic registered but currently non-functional)

	local_crown_estate_power = -1.0

	local_levy_size_modifier = -1.0
	local_manpower_modifier = -1.0
	local_sailors_modifier = -1.0
}

# total_population: restored to vanilla. LB delta handled by sol_lb_population_delta.
TRY_REPLACE:total_population = {
	game_data = {
		category = location
	}
	supply_limit = 0.02
	blockade_force_required = 0.02

	local_max_rgo_size = 0.025   # Vanilla (delta -0.005 handled by sol_lb_population_delta)

	free_building_levels = 0.05
}

# looted: restored to vanilla (with prosperity_decay). HWC mechanism-change handled by sol_hwc_looted_prosperity.
TRY_REPLACE:looted = {
	game_data = {
		category = location
	}
	hostile_food_multiplier = -0.10         # Same as vanilla
	local_population_growth = -0.005        # Same as vanilla
	local_prosperity_decay = 0.1            # Vanilla (neutralized by sol_hwc_looted_prosperity when hwc ON)
	local_monthly_development_modifier = -0.25 # Same as vanilla
	max_attrition = 5
}

## Little Ice Age modifiers (location): restored to vanilla values.
## The SOL relief is now applied by CMM auto_modifiers in SOL_little_ice_age.txt when sol__lia is ON.

TRY_REPLACE:harsh_winters_modifier = {
	game_data = {
		category = location
	}
	local_food_capacity_modifier = -0.25 # Vanilla (relief +0.10 handled by sol_lia_harsh_winters_relief)
	local_monthly_food_modifier = -0.25  # Vanilla (relief +0.15 handled by sol_lia_harsh_winters_relief)
}

TRY_REPLACE:reduced_wool_production_modifier = {
	game_data = {
		category = location
		decaying = yes
	}

	local_raw_material_output = -1 # Vanilla (relief +0.5 handled by sol_lia_wool_relief)
}

TRY_REPLACE:fewer_fisheries_modifier = {
	game_data = {
		category = location
		decaying = yes
	}

	local_raw_material_output = -0.33  # Vanilla (relief +0.08 handled by sol_lia_fisheries_relief)
	local_monthly_food_modifier = -0.25 # Vanilla (relief +0.10 handled by sol_lia_fisheries_relief)
}
