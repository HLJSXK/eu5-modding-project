# SOL difficulty tweaks have been moved to auto_modifiers.
# See: src/stable/in_game/common/auto_modifiers/SOL_difficulty.txt
#
# Vanilla difficulty static modifiers apply normally (no TRY_REPLACE).
# The SOL auto_modifiers apply a negative delta to tax_income_efficiency
# when the CMM "Difficulty tax nerf" toggle is ON.
