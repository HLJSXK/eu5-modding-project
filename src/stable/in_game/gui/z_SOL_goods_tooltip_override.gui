# ============================================================
# z_SOL_goods_tooltip_override.gui
# Overrides 4 vanilla goods tooltip templates to inject substitute group info.
#
# Templates overridden:
#   Goods_tooltip                — generic goods tooltip (Goods scope)
#   SpecificGoodsMarket_tooltip  — market-context goods tooltip (Goods scope)
#   goods_market_price_tooltip   — market price chart tooltip (GoodsMarketEntry scope)
#   GoodsPriceOnMarket_tooltip   — price-on-market tooltip (GoodsPriceOnMarketWrap scope)
#
# All vanilla blockoverrides are replicated unchanged.
# Adds blockoverride "extra_tooltip_content" → SOL_goods_substitute_group_block
# Non-Goods scopes use widget { datacontext = "[X.GetGoods]" } to switch to Goods scope.
#
# AUTO-GENERATED (scaffold + body) by scripts/gen_scarcity.py — do not edit by hand.
# ============================================================

template Goods_tooltip {
	ContextualTooltipType = {
		blockoverride "title_icon" {
			button = {
				using = tooltip_title_icon_size
				texture = "[GetGoodsIcon(Goods.Self)]"
				onclick = "[ShowGoods(Goods.Self)]"
				tooltipwidget = {
					LeftClickButtonTooltip = {
						blockoverride "onclick_text" { text = "OPEN_GOOD_PANEL" }
					}
				}
			}
		}

		blockoverride "title_text" {
			text = "[Goods.GetNameWithNoTooltip]"
		}

		blockoverride "concept_link" {
			text = [goods|e]
		}

		blockoverride "title_button" {
			custom_map_mode_button = {
				datacontext = "[GetMapMode('reactive_trade_good')]"

				blockoverride "on_action" {
					on_action = "[ShowGoodsReactive(Goods.Self)]"
				}

				blockoverride "icon" {
					texture = "[GetGoodsIcon(Goods.Self)]"
				}
			}
		}

		blockoverride "tooltip_content" {
			using = goods_details

			TooltipFlavorTextBlock = {
				blockoverride "text" {
					text = "[Goods.GetFlavorText]"
				}
			}
		}

		# ── Substitute group info ─────────────────────────────────────
		blockoverride "extra_tooltip_content" {
			using = SOL_goods_substitute_group_block
		}
	}
}

template SOL_goods_substitute_group_block {
	# One widget per good — exactly one visible at a time.
	# Visibility check: EqualTo_string(Goods.GetKey, 'X')
	# Pattern confirmed from battle_lateralview.gui:609 (EqualTo_string(CombatModifier.GetKey, 'dice'))
	# AUTO-GENERATED — do not edit by hand.
	# --- Alcoholic Drinks ---
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'wine')]"
		blockoverride "text" { text = "SOL_good_subst_group_wine" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'liquor')]"
		blockoverride "text" { text = "SOL_good_subst_group_liquor" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'beer')]"
		blockoverride "text" { text = "SOL_good_subst_group_beer" }
	}

	# --- Textiles ---
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'fur')]"
		blockoverride "text" { text = "SOL_good_subst_group_fur" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'cloth')]"
		blockoverride "text" { text = "SOL_good_subst_group_cloth" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'fine_cloth')]"
		blockoverride "text" { text = "SOL_good_subst_group_fine_cloth" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'leather')]"
		blockoverride "text" { text = "SOL_good_subst_group_leather" }
	}

	# --- Knowledge Goods ---
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'beeswax')]"
		blockoverride "text" { text = "SOL_good_subst_group_beeswax" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'paper')]"
		blockoverride "text" { text = "SOL_good_subst_group_paper" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'books')]"
		blockoverride "text" { text = "SOL_good_subst_group_books" }
	}

	# --- Precious Goods ---
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'goods_gold')]"
		blockoverride "text" { text = "SOL_good_subst_group_goods_gold" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'silver')]"
		blockoverride "text" { text = "SOL_good_subst_group_silver" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'jewelry')]"
		blockoverride "text" { text = "SOL_good_subst_group_jewelry" }
	}

	# --- Treasures ---
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'amber')]"
		blockoverride "text" { text = "SOL_good_subst_group_amber" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'gems')]"
		blockoverride "text" { text = "SOL_good_subst_group_gems" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'ivory')]"
		blockoverride "text" { text = "SOL_good_subst_group_ivory" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'pearls')]"
		blockoverride "text" { text = "SOL_good_subst_group_pearls" }
	}

	# --- Ritual Goods ---
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'incense')]"
		blockoverride "text" { text = "SOL_good_subst_group_incense" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'medicaments')]"
		blockoverride "text" { text = "SOL_good_subst_group_medicaments" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'mercury')]"
		blockoverride "text" { text = "SOL_good_subst_group_mercury" }
	}

	# --- Stimulants ---
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'sugar')]"
		blockoverride "text" { text = "SOL_good_subst_group_sugar" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'tobacco')]"
		blockoverride "text" { text = "SOL_good_subst_group_tobacco" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'tea')]"
		blockoverride "text" { text = "SOL_good_subst_group_tea" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'cocoa')]"
		blockoverride "text" { text = "SOL_good_subst_group_cocoa" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'coffee')]"
		blockoverride "text" { text = "SOL_good_subst_group_coffee" }
	}

	# --- Spices ---
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'saffron')]"
		blockoverride "text" { text = "SOL_good_subst_group_saffron" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'pepper')]"
		blockoverride "text" { text = "SOL_good_subst_group_pepper" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'cloves')]"
		blockoverride "text" { text = "SOL_good_subst_group_cloves" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'chili')]"
		blockoverride "text" { text = "SOL_good_subst_group_chili" }
	}

	# --- Staple Food ---
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'wheat')]"
		blockoverride "text" { text = "SOL_good_subst_group_wheat" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'rice')]"
		blockoverride "text" { text = "SOL_good_subst_group_rice" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'millet')]"
		blockoverride "text" { text = "SOL_good_subst_group_millet" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'maize')]"
		blockoverride "text" { text = "SOL_good_subst_group_maize" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'potato')]"
		blockoverride "text" { text = "SOL_good_subst_group_potato" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'legumes')]"
		blockoverride "text" { text = "SOL_good_subst_group_legumes" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'olives')]"
		blockoverride "text" { text = "SOL_good_subst_group_olives" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'fruit')]"
		blockoverride "text" { text = "SOL_good_subst_group_fruit" }
	}

	# --- Protein Food ---
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'fish')]"
		blockoverride "text" { text = "SOL_good_subst_group_fish" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'wild_game')]"
		blockoverride "text" { text = "SOL_good_subst_group_wild_game" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'livestock')]"
		blockoverride "text" { text = "SOL_good_subst_group_livestock" }
	}

	# --- Military Goods ---
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'horses')]"
		blockoverride "text" { text = "SOL_good_subst_group_horses" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'elephants')]"
		blockoverride "text" { text = "SOL_good_subst_group_elephants" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'weaponry')]"
		blockoverride "text" { text = "SOL_good_subst_group_weaponry" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'firearms')]"
		blockoverride "text" { text = "SOL_good_subst_group_firearms" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'coal')]"
		blockoverride "text" { text = "SOL_good_subst_group_coal" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'salt')]"
		blockoverride "text" { text = "SOL_good_subst_group_salt" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'victuals')]"
		blockoverride "text" { text = "SOL_good_subst_group_victuals" }
	}

	# --- Household Goods ---
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'lumber')]"
		blockoverride "text" { text = "SOL_good_subst_group_lumber" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'masonry')]"
		blockoverride "text" { text = "SOL_good_subst_group_masonry" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'tools')]"
		blockoverride "text" { text = "SOL_good_subst_group_tools" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'pottery')]"
		blockoverride "text" { text = "SOL_good_subst_group_pottery" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'furniture')]"
		blockoverride "text" { text = "SOL_good_subst_group_furniture" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'porcelain')]"
		blockoverride "text" { text = "SOL_good_subst_group_porcelain" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'lacquerware')]"
		blockoverride "text" { text = "SOL_good_subst_group_lacquerware" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'marble')]"
		blockoverride "text" { text = "SOL_good_subst_group_marble" }
	}
	TooltipTextBlock = {
		visible = "[EqualTo_string(Goods.GetKey, 'glass')]"
		blockoverride "text" { text = "SOL_good_subst_group_glass" }
	}
}

template SpecificGoodsMarket_tooltip {
	ContextualTooltipType = {

		blockoverride "title_icon" {
			button = {
				using = tooltip_title_icon_size
				texture = "[GetGoodsIcon(Goods.Self)]"
				tooltipwidget = {
					using = Goods_tooltip
				}
				action_tooltip = {
					click_type = left
					click_mode = single
					title = "OPEN_GOOD_PANEL"
					on_action = "[ShowGoods(Goods.Self)]"
				}
			}
		}

		blockoverride "title_text" {
			text = "[Goods.GetNameWithNoTooltip]"
		}

		blockoverride "concept_link" {
			text = [goods|E]
		}

		blockoverride "title_button" {
			custom_map_mode_button = {
				datacontext = "[GetMapMode('reactive_trade_good')]"

				blockoverride "on_action" {
					on_action = "[ShowGoodsReactive(Goods.Self)]"
				}

				blockoverride "icon" {
					texture = "[GetGoodsIcon(Goods.Self)]"
				}
			}
		}

		blockoverride "tooltip_content" {
			using = goods_details
			datacontext = "[Market.GetMarketEntry(Goods.Self)]"

			using = SpecificGoodsFromMarketContent
			blockoverride "data_from" {
				datacontext = "[Market]"
			}

			block "good_details_extra_info" {}

			TooltipFlavorTextBlock = {
				textcontext = "[Goods.GetFlavorText]"
			}
		}

		# ── Substitute group info ─────────────────────────────────────
		blockoverride "extra_tooltip_content" {
			using = SOL_goods_substitute_group_block
		}
	}
}

template goods_market_price_tooltip {
	ContextualTooltipType = {
		blockoverride "title_icon" {
			button = {
				datacontext = "[GoodsMarketEntry.GetGoods]"
				using = tooltip_title_icon_size
				texture = "[GetGoodsIcon(GoodsMarketEntry.GetGoods)]"
				tooltipwidget = {
					using = Goods_tooltip
				}
				action_tooltip = {
					click_type = left
					click_mode = single
					title = "OPEN_GOOD_PANEL"
					on_action = "[ShowGoods(GoodsMarketEntry.GetGoods)]"
				}
			}
		}

		blockoverride "title_text" {
			text = "GOODS_MARKET_ENTRY_TITLE"
		}

		blockoverride "concept_link" {
			text = "[market_price|e]"
		}

		blockoverride "tooltip_content" {
			using = goods_market_details
		}

		# ── Substitute group info (GoodsMarketEntry scope → Goods via datacontext) ────
		blockoverride "extra_tooltip_content" {
			widget = {
				datacontext = "[GoodsMarketEntry.GetGoods]"
				using = SOL_goods_substitute_group_block
			}
		}
	}
}

template GoodsPriceOnMarket_tooltip {
	ContextualTooltipType = {
		blockoverride "title_icon_texture" {
			texture = "[GetGoodsIcon(GoodsPriceOnMarketWrap.GetGoods)]"
		}

		blockoverride "title_text" {
			text = "[GoodsPriceOnMarketWrap.GetName]"
		}

		blockoverride "concept_link" {
			text = "[market_price|e]"
		}

		blockoverride "tooltip_content" {
			datacontext = "[GoodsPriceOnMarketWrap.GetGoodsMarketEntry]"
			datacontext = "[GoodsPriceOnMarketWrap.GetMarket]"
			using = goods_market_details
		}

		# ── Substitute group info (GoodsPriceOnMarketWrap scope → Goods via datacontext) ────
		blockoverride "extra_tooltip_content" {
			widget = {
				datacontext = "[GoodsPriceOnMarketWrap.GetGoods]"
				using = SOL_goods_substitute_group_block
			}
		}
	}
}
