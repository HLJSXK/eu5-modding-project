# Root scope: country.
sol_register_cmf_mod = {
	# Features (feat) Tab
	## SOL-based pop demands (sol) Group
	cmm_register_global_bool_setting = {
		mod_id = sol
		setting_id = sol_on
		tab_id = feat
		group_id = sol
		default_value = 1
	}

	## Anti-snowballing (as) Group
	cmm_register_global_bool_setting = {
		mod_id = sol
		setting_id = as_on
		tab_id = feat
		group_id = as
		default_value = 1
	}
}

# Callback handler for cmf_on_callback.
# var:cmf_callback is the flag of the setting, alert, or action bar element that was interacted with.
# Scope: country
sol_handle_cmf_callback = {
	switch = {
		trigger = var:cmf_callback
		# flag:sol__my_setting = {
		# 	# Custom side effects when this setting changes
		# }
	}
}
