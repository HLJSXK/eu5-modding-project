### ============================================================
### SECTION: Substitute Group Scarcity Indicators (GUI helpers)
### ============================================================
# Each value returns 1 if any good in the group is currently scarce
# at the evaluated location (has local_<good>_scarce variable set).
#
# Used by location_window.gui demography_item blockoverride to drive
# visibility of per-stratum substitute-goods icons in the Population tab.
#
# Scope when evaluated: location (via Location.MakeScope.ScriptValue)

sol_grp_precious_scarce = {
    value = 0
    if = {
        limit = {
            OR = {
                has_variable = local_goods_gold_scarce
                has_variable = local_silver_scarce
                has_variable = local_pearls_scarce
                has_variable = local_amber_scarce
                has_variable = local_gems_scarce
                has_variable = local_ivory_scarce
            }
        }
        value = 1
    }
}

sol_grp_stimulants_scarce = {
    value = 0
    if = {
        limit = {
            OR = {
                has_variable = local_sugar_scarce
                has_variable = local_tobacco_scarce
                has_variable = local_tea_scarce
                has_variable = local_cocoa_scarce
                has_variable = local_coffee_scarce
            }
        }
        value = 1
    }
}

sol_grp_ritual_scarce = {
    value = 0
    if = {
        limit = {
            OR = {
                has_variable = local_incense_scarce
                has_variable = local_medicaments_scarce
                has_variable = local_mercury_scarce
            }
        }
        value = 1
    }
}

sol_grp_knowledge_scarce = {
    value = 0
    if = {
        limit = {
            OR = {
                has_variable = local_paper_scarce
                has_variable = local_books_scarce
                has_variable = local_beeswax_scarce
            }
        }
        value = 1
    }
}

sol_grp_textiles_scarce = {
    value = 0
    if = {
        limit = {
            OR = {
                has_variable = local_fur_scarce
                has_variable = local_cloth_scarce
                has_variable = local_fine_cloth_scarce
                has_variable = local_jewelry_scarce
            }
        }
        value = 1
    }
}

sol_grp_spices_scarce = {
    value = 0
    if = {
        limit = {
            OR = {
                has_variable = local_saffron_scarce
                has_variable = local_pepper_scarce
                has_variable = local_cloves_scarce
                has_variable = local_chili_scarce
            }
        }
        value = 1
    }
}

sol_grp_staple_scarce = {
    value = 0
    if = {
        limit = {
            OR = {
                has_variable = local_wheat_scarce
                has_variable = local_rice_scarce
                has_variable = local_millet_scarce
                has_variable = local_maize_scarce
                has_variable = local_potato_scarce
                has_variable = local_legumes_scarce
                has_variable = local_olives_scarce
                has_variable = local_fruit_scarce
            }
        }
        value = 1
    }
}

sol_grp_protein_scarce = {
    value = 0
    if = {
        limit = {
            OR = {
                has_variable = local_fish_scarce
                has_variable = local_wild_game_scarce
                has_variable = local_livestock_scarce
            }
        }
        value = 1
    }
}

sol_grp_alcohol_scarce = {
    value = 0
    if = {
        limit = {
            OR = {
                has_variable = local_wine_scarce
                has_variable = local_liquor_scarce
                has_variable = local_beer_scarce
            }
        }
        value = 1
    }
}

sol_grp_military_scarce = {
    value = 0
    if = {
        limit = {
            OR = {
                has_variable = local_horses_scarce
                has_variable = local_elephants_scarce
                has_variable = local_weaponry_scarce
                has_variable = local_firearms_scarce
                has_variable = local_coal_scarce
                has_variable = local_salt_scarce
                has_variable = local_victuals_scarce
            }
        }
        value = 1
    }
}
