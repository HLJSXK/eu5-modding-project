# Location-scope demand scale values — one entry per (strata, group) pair.
# Formula: demand_scale = (savings_pressure + 1) × gdp_per_capita × budget_share / P
# Tribesmen use a simplified form: savings_pressure + 1 (no ratio term).
#
# These are the canonical implementations. The pop-scope file (z_SOL_group_demand_scales.txt)
# delegates to these values by routing on pop_type.
#
# Variable naming follows existing conventions exactly:
#   nobles   : local_nobles_savings_pressure / local_noble_gdp_per_capita_display
#   clergy   : local_clergy_savings_pressure / local_clergy_gdp_per_capita_display
#   burghers : local_burghers_savings_pressure / local_burghers_gdp_per_capita_display
#   commoners: local_commoner_savings_pressure / local_commoner_gdp_per_capita_display
#   tribesmen: local_tribesmen_savings_pressure

# ──────────────────────────────────────────────────────────────
#  GROUP: alcohol
# ──────────────────────────────────────────────────────────────
local_nobles_alcohol_demand_scale = {
	value = local_nobles_savings_pressure
	add = 1
	multiply = local_noble_gdp_per_capita_display
	multiply = local_nobles_alcohol_budget_share
	divide = local_nobles_alcohol_P
	min = 0
}
local_clergy_alcohol_demand_scale = {
	value = local_clergy_savings_pressure
	add = 1
	multiply = local_clergy_gdp_per_capita_display
	multiply = local_clergy_alcohol_budget_share
	divide = local_clergy_alcohol_P
	min = 0
}
local_burghers_alcohol_demand_scale = {
	value = local_burghers_savings_pressure
	add = 1
	multiply = local_burghers_gdp_per_capita_display
	multiply = local_burghers_alcohol_budget_share
	divide = local_burghers_alcohol_P
	min = 0
}
local_commoners_alcohol_demand_scale = {
	value = local_commoner_savings_pressure
	add = 1
	multiply = local_commoner_gdp_per_capita_display
	multiply = local_commoners_alcohol_budget_share
	divide = local_commoners_alcohol_P
	min = 0
}
local_tribesmen_alcohol_demand_scale = {
	value = local_tribesmen_savings_pressure
	add = 1
	min = 0
}

# ──────────────────────────────────────────────────────────────
#  GROUP: textiles
# ──────────────────────────────────────────────────────────────
local_nobles_textiles_demand_scale = {
	value = local_nobles_savings_pressure
	add = 1
	multiply = local_noble_gdp_per_capita_display
	multiply = local_nobles_textiles_budget_share
	divide = local_nobles_textiles_P
	min = 0
}
local_clergy_textiles_demand_scale = {
	value = local_clergy_savings_pressure
	add = 1
	multiply = local_clergy_gdp_per_capita_display
	multiply = local_clergy_textiles_budget_share
	divide = local_clergy_textiles_P
	min = 0
}
local_burghers_textiles_demand_scale = {
	value = local_burghers_savings_pressure
	add = 1
	multiply = local_burghers_gdp_per_capita_display
	multiply = local_burghers_textiles_budget_share
	divide = local_burghers_textiles_P
	min = 0
}
local_commoners_textiles_demand_scale = {
	value = local_commoner_savings_pressure
	add = 1
	multiply = local_commoner_gdp_per_capita_display
	multiply = local_commoners_textiles_budget_share
	divide = local_commoners_textiles_P
	min = 0
}
local_tribesmen_textiles_demand_scale = {
	value = local_tribesmen_savings_pressure
	add = 1
	min = 0
}

# ──────────────────────────────────────────────────────────────
#  GROUP: knowledge
# ──────────────────────────────────────────────────────────────
local_nobles_knowledge_demand_scale = {
	value = local_nobles_savings_pressure
	add = 1
	multiply = local_noble_gdp_per_capita_display
	multiply = local_nobles_knowledge_budget_share
	divide = local_nobles_knowledge_P
	min = 0
}
local_clergy_knowledge_demand_scale = {
	value = local_clergy_savings_pressure
	add = 1
	multiply = local_clergy_gdp_per_capita_display
	multiply = local_clergy_knowledge_budget_share
	divide = local_clergy_knowledge_P
	min = 0
}
local_burghers_knowledge_demand_scale = {
	value = local_burghers_savings_pressure
	add = 1
	multiply = local_burghers_gdp_per_capita_display
	multiply = local_burghers_knowledge_budget_share
	divide = local_burghers_knowledge_P
	min = 0
}
local_commoners_knowledge_demand_scale = {
	value = local_commoner_savings_pressure
	add = 1
	multiply = local_commoner_gdp_per_capita_display
	multiply = local_commoners_knowledge_budget_share
	divide = local_commoners_knowledge_P
	min = 0
}
local_tribesmen_knowledge_demand_scale = {
	value = local_tribesmen_savings_pressure
	add = 1
	min = 0
}

# ──────────────────────────────────────────────────────────────
#  GROUP: precious
# ──────────────────────────────────────────────────────────────
local_nobles_precious_demand_scale = {
	value = local_nobles_savings_pressure
	add = 1
	multiply = local_noble_gdp_per_capita_display
	multiply = local_nobles_precious_budget_share
	divide = local_nobles_precious_P
	min = 0
}
local_clergy_precious_demand_scale = {
	value = local_clergy_savings_pressure
	add = 1
	multiply = local_clergy_gdp_per_capita_display
	multiply = local_clergy_precious_budget_share
	divide = local_clergy_precious_P
	min = 0
}
local_burghers_precious_demand_scale = {
	value = local_burghers_savings_pressure
	add = 1
	multiply = local_burghers_gdp_per_capita_display
	multiply = local_burghers_precious_budget_share
	divide = local_burghers_precious_P
	min = 0
}
local_commoners_precious_demand_scale = {
	value = local_commoner_savings_pressure
	add = 1
	multiply = local_commoner_gdp_per_capita_display
	multiply = local_commoners_precious_budget_share
	divide = local_commoners_precious_P
	min = 0
}
local_tribesmen_precious_demand_scale = {
	value = local_tribesmen_savings_pressure
	add = 1
	min = 0
}

# ──────────────────────────────────────────────────────────────
#  GROUP: ritual
# ──────────────────────────────────────────────────────────────
local_nobles_ritual_demand_scale = {
	value = local_nobles_savings_pressure
	add = 1
	multiply = local_noble_gdp_per_capita_display
	multiply = local_nobles_ritual_budget_share
	divide = local_nobles_ritual_P
	min = 0
}
local_clergy_ritual_demand_scale = {
	value = local_clergy_savings_pressure
	add = 1
	multiply = local_clergy_gdp_per_capita_display
	multiply = local_clergy_ritual_budget_share
	divide = local_clergy_ritual_P
	min = 0
}
local_burghers_ritual_demand_scale = {
	value = local_burghers_savings_pressure
	add = 1
	multiply = local_burghers_gdp_per_capita_display
	multiply = local_burghers_ritual_budget_share
	divide = local_burghers_ritual_P
	min = 0
}
local_commoners_ritual_demand_scale = {
	value = local_commoner_savings_pressure
	add = 1
	multiply = local_commoner_gdp_per_capita_display
	multiply = local_commoners_ritual_budget_share
	divide = local_commoners_ritual_P
	min = 0
}
local_tribesmen_ritual_demand_scale = {
	value = local_tribesmen_savings_pressure
	add = 1
	min = 0
}

# ──────────────────────────────────────────────────────────────
#  GROUP: stimulants
# ──────────────────────────────────────────────────────────────
local_nobles_stimulants_demand_scale = {
	value = local_nobles_savings_pressure
	add = 1
	multiply = local_noble_gdp_per_capita_display
	multiply = local_nobles_stimulants_budget_share
	divide = local_nobles_stimulants_P
	min = 0
}
local_clergy_stimulants_demand_scale = {
	value = local_clergy_savings_pressure
	add = 1
	multiply = local_clergy_gdp_per_capita_display
	multiply = local_clergy_stimulants_budget_share
	divide = local_clergy_stimulants_P
	min = 0
}
local_burghers_stimulants_demand_scale = {
	value = local_burghers_savings_pressure
	add = 1
	multiply = local_burghers_gdp_per_capita_display
	multiply = local_burghers_stimulants_budget_share
	divide = local_burghers_stimulants_P
	min = 0
}
local_commoners_stimulants_demand_scale = {
	value = local_commoner_savings_pressure
	add = 1
	multiply = local_commoner_gdp_per_capita_display
	multiply = local_commoners_stimulants_budget_share
	divide = local_commoners_stimulants_P
	min = 0
}
local_tribesmen_stimulants_demand_scale = {
	value = local_tribesmen_savings_pressure
	add = 1
	min = 0
}

# ──────────────────────────────────────────────────────────────
#  GROUP: spices
# ──────────────────────────────────────────────────────────────
local_nobles_spices_demand_scale = {
	value = local_nobles_savings_pressure
	add = 1
	multiply = local_noble_gdp_per_capita_display
	multiply = local_nobles_spices_budget_share
	divide = local_nobles_spices_P
	min = 0
}
local_clergy_spices_demand_scale = {
	value = local_clergy_savings_pressure
	add = 1
	multiply = local_clergy_gdp_per_capita_display
	multiply = local_clergy_spices_budget_share
	divide = local_clergy_spices_P
	min = 0
}
local_burghers_spices_demand_scale = {
	value = local_burghers_savings_pressure
	add = 1
	multiply = local_burghers_gdp_per_capita_display
	multiply = local_burghers_spices_budget_share
	divide = local_burghers_spices_P
	min = 0
}
local_commoners_spices_demand_scale = {
	value = local_commoner_savings_pressure
	add = 1
	multiply = local_commoner_gdp_per_capita_display
	multiply = local_commoners_spices_budget_share
	divide = local_commoners_spices_P
	min = 0
}
local_tribesmen_spices_demand_scale = {
	value = local_tribesmen_savings_pressure
	add = 1
	min = 0
}

# ──────────────────────────────────────────────────────────────
#  GROUP: staple
# ──────────────────────────────────────────────────────────────
local_nobles_staple_demand_scale = {
	value = local_nobles_savings_pressure
	add = 1
	multiply = local_noble_gdp_per_capita_display
	multiply = local_nobles_staple_budget_share
	divide = local_nobles_staple_P
	min = 0
}
local_clergy_staple_demand_scale = {
	value = local_clergy_savings_pressure
	add = 1
	multiply = local_clergy_gdp_per_capita_display
	multiply = local_clergy_staple_budget_share
	divide = local_clergy_staple_P
	min = 0
}
local_burghers_staple_demand_scale = {
	value = local_burghers_savings_pressure
	add = 1
	multiply = local_burghers_gdp_per_capita_display
	multiply = local_burghers_staple_budget_share
	divide = local_burghers_staple_P
	min = 0
}
local_commoners_staple_demand_scale = {
	value = local_commoner_savings_pressure
	add = 1
	multiply = local_commoner_gdp_per_capita_display
	multiply = local_commoners_staple_budget_share
	divide = local_commoners_staple_P
	min = 0
}
local_tribesmen_staple_demand_scale = {
	value = local_tribesmen_savings_pressure
	add = 1
	min = 0
}

# ──────────────────────────────────────────────────────────────
#  GROUP: protein
# ──────────────────────────────────────────────────────────────
local_nobles_protein_demand_scale = {
	value = local_nobles_savings_pressure
	add = 1
	multiply = local_noble_gdp_per_capita_display
	multiply = local_nobles_protein_budget_share
	divide = local_nobles_protein_P
	min = 0
}
local_clergy_protein_demand_scale = {
	value = local_clergy_savings_pressure
	add = 1
	multiply = local_clergy_gdp_per_capita_display
	multiply = local_clergy_protein_budget_share
	divide = local_clergy_protein_P
	min = 0
}
local_burghers_protein_demand_scale = {
	value = local_burghers_savings_pressure
	add = 1
	multiply = local_burghers_gdp_per_capita_display
	multiply = local_burghers_protein_budget_share
	divide = local_burghers_protein_P
	min = 0
}
local_commoners_protein_demand_scale = {
	value = local_commoner_savings_pressure
	add = 1
	multiply = local_commoner_gdp_per_capita_display
	multiply = local_commoners_protein_budget_share
	divide = local_commoners_protein_P
	min = 0
}
local_tribesmen_protein_demand_scale = {
	value = local_tribesmen_savings_pressure
	add = 1
	min = 0
}

# ──────────────────────────────────────────────────────────────
#  GROUP: military
# ──────────────────────────────────────────────────────────────
local_nobles_military_demand_scale = {
	value = local_nobles_savings_pressure
	add = 1
	multiply = local_noble_gdp_per_capita_display
	multiply = local_nobles_military_budget_share
	divide = local_nobles_military_P
	min = 0
}
local_clergy_military_demand_scale = {
	value = local_clergy_savings_pressure
	add = 1
	multiply = local_clergy_gdp_per_capita_display
	multiply = local_clergy_military_budget_share
	divide = local_clergy_military_P
	min = 0
}
local_burghers_military_demand_scale = {
	value = local_burghers_savings_pressure
	add = 1
	multiply = local_burghers_gdp_per_capita_display
	multiply = local_burghers_military_budget_share
	divide = local_burghers_military_P
	min = 0
}
local_commoners_military_demand_scale = {
	value = local_commoner_savings_pressure
	add = 1
	multiply = local_commoner_gdp_per_capita_display
	multiply = local_commoners_military_budget_share
	divide = local_commoners_military_P
	min = 0
}
local_tribesmen_military_demand_scale = {
	value = local_tribesmen_savings_pressure
	add = 1
	min = 0
}
