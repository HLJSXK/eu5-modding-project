# Intermediate demand base values — one entry per (strata, group) pair.
# Formula (normal):  demand_base = gdp * alpha/P + demand_offset
# Formula (P==0):    demand_base = 1  (neutral; group not priced for this strata)
# Referenced by z_SOL_group_demand_scales_location.txt via multiply = demand_base.
#
# Generated by engel_export.py — DO NOT EDIT MANUALLY

# ──────────────────────────────────────────────────────────────
#  GROUP: basic_clothing
# ──────────────────────────────────────────────────────────────
local_nobles_basic_clothing_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_basic_clothing_budget_share
	divide = local_nobles_basic_clothing_P
	add = local_nobles_basic_clothing_demand_offset
}
local_clergy_basic_clothing_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_basic_clothing_budget_share
	divide = local_clergy_basic_clothing_P
	add = local_clergy_basic_clothing_demand_offset
}
local_burghers_basic_clothing_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_basic_clothing_budget_share
	divide = local_burghers_basic_clothing_P
	add = local_burghers_basic_clothing_demand_offset
}
local_commoners_basic_clothing_demand_base = {
	value = local_commoner_gdp_per_capita_display
	multiply = local_commoners_basic_clothing_budget_share
	divide = local_commoners_basic_clothing_P
	add = local_commoners_basic_clothing_demand_offset
}
# tribesmen: no income term — hardcoded neutral
local_tribesmen_basic_clothing_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: crude_goods
# ──────────────────────────────────────────────────────────────
local_nobles_crude_goods_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_crude_goods_budget_share
	divide = local_nobles_crude_goods_P
	add = local_nobles_crude_goods_demand_offset
}
local_clergy_crude_goods_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_crude_goods_budget_share
	divide = local_clergy_crude_goods_P
	add = local_clergy_crude_goods_demand_offset
}
local_burghers_crude_goods_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_crude_goods_budget_share
	divide = local_burghers_crude_goods_P
	add = local_burghers_crude_goods_demand_offset
}
local_commoners_crude_goods_demand_base = {
	value = local_commoner_gdp_per_capita_display
	multiply = local_commoners_crude_goods_budget_share
	divide = local_commoners_crude_goods_P
	add = local_commoners_crude_goods_demand_offset
}
# tribesmen: no income term — hardcoded neutral
local_tribesmen_crude_goods_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: staple
# ──────────────────────────────────────────────────────────────
local_nobles_staple_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_staple_budget_share
	divide = local_nobles_staple_P
	add = local_nobles_staple_demand_offset
}
local_clergy_staple_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_staple_budget_share
	divide = local_clergy_staple_P
	add = local_clergy_staple_demand_offset
}
local_burghers_staple_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_staple_budget_share
	divide = local_burghers_staple_P
	add = local_burghers_staple_demand_offset
}
local_commoners_staple_demand_base = {
	value = local_commoner_gdp_per_capita_display
	multiply = local_commoners_staple_budget_share
	divide = local_commoners_staple_P
	add = local_commoners_staple_demand_offset
}
# tribesmen: no income term — hardcoded neutral
local_tribesmen_staple_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: condiments
# ──────────────────────────────────────────────────────────────
local_nobles_condiments_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_condiments_budget_share
	divide = local_nobles_condiments_P
	add = local_nobles_condiments_demand_offset
}
local_clergy_condiments_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_condiments_budget_share
	divide = local_clergy_condiments_P
	add = local_clergy_condiments_demand_offset
}
local_burghers_condiments_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_condiments_budget_share
	divide = local_burghers_condiments_P
	add = local_burghers_condiments_demand_offset
}
local_commoners_condiments_demand_base = {
	value = local_commoner_gdp_per_capita_display
	multiply = local_commoners_condiments_budget_share
	divide = local_commoners_condiments_P
	add = local_commoners_condiments_demand_offset
}
# tribesmen: no income term — hardcoded neutral
local_tribesmen_condiments_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: heating
# ──────────────────────────────────────────────────────────────
local_nobles_heating_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_heating_budget_share
	divide = local_nobles_heating_P
	add = local_nobles_heating_demand_offset
}
local_clergy_heating_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_heating_budget_share
	divide = local_clergy_heating_P
	add = local_clergy_heating_demand_offset
}
local_burghers_heating_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_heating_budget_share
	divide = local_burghers_heating_P
	add = local_burghers_heating_demand_offset
}
local_commoners_heating_demand_base = {
	value = local_commoner_gdp_per_capita_display
	multiply = local_commoners_heating_budget_share
	divide = local_commoners_heating_P
	add = local_commoners_heating_demand_offset
}
# tribesmen: no income term — hardcoded neutral
local_tribesmen_heating_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: household
# ──────────────────────────────────────────────────────────────
local_nobles_household_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_household_budget_share
	divide = local_nobles_household_P
	add = local_nobles_household_demand_offset
}
local_clergy_household_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_household_budget_share
	divide = local_clergy_household_P
	add = local_clergy_household_demand_offset
}
local_burghers_household_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_household_budget_share
	divide = local_burghers_household_P
	add = local_burghers_household_demand_offset
}
local_commoners_household_demand_base = {
	value = local_commoner_gdp_per_capita_display
	multiply = local_commoners_household_budget_share
	divide = local_commoners_household_P
	add = local_commoners_household_demand_offset
}
# tribesmen: no income term — hardcoded neutral
local_tribesmen_household_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: standard_clothing
# ──────────────────────────────────────────────────────────────
local_nobles_standard_clothing_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_standard_clothing_budget_share
	divide = local_nobles_standard_clothing_P
	add = local_nobles_standard_clothing_demand_offset
}
local_clergy_standard_clothing_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_standard_clothing_budget_share
	divide = local_clergy_standard_clothing_P
	add = local_clergy_standard_clothing_demand_offset
}
local_burghers_standard_clothing_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_standard_clothing_budget_share
	divide = local_burghers_standard_clothing_P
	add = local_burghers_standard_clothing_demand_offset
}
local_commoners_standard_clothing_demand_base = {
	value = local_commoner_gdp_per_capita_display
	multiply = local_commoners_standard_clothing_budget_share
	divide = local_commoners_standard_clothing_P
	add = local_commoners_standard_clothing_demand_offset
}
# tribesmen: no income term — hardcoded neutral
local_tribesmen_standard_clothing_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: intoxicants
# ──────────────────────────────────────────────────────────────
local_nobles_intoxicants_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_intoxicants_budget_share
	divide = local_nobles_intoxicants_P
	add = local_nobles_intoxicants_demand_offset
}
local_clergy_intoxicants_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_intoxicants_budget_share
	divide = local_clergy_intoxicants_P
	add = local_clergy_intoxicants_demand_offset
}
local_burghers_intoxicants_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_intoxicants_budget_share
	divide = local_burghers_intoxicants_P
	add = local_burghers_intoxicants_demand_offset
}
local_commoners_intoxicants_demand_base = {
	value = local_commoner_gdp_per_capita_display
	multiply = local_commoners_intoxicants_budget_share
	divide = local_commoners_intoxicants_P
	add = local_commoners_intoxicants_demand_offset
}
# tribesmen: no income term — hardcoded neutral
local_tribesmen_intoxicants_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: luxury_drinks
# ──────────────────────────────────────────────────────────────
local_nobles_luxury_drinks_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_luxury_drinks_budget_share
	divide = local_nobles_luxury_drinks_P
	add = local_nobles_luxury_drinks_demand_offset
}
local_clergy_luxury_drinks_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_luxury_drinks_budget_share
	divide = local_clergy_luxury_drinks_P
	add = local_clergy_luxury_drinks_demand_offset
}
local_burghers_luxury_drinks_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_luxury_drinks_budget_share
	divide = local_burghers_luxury_drinks_P
	add = local_burghers_luxury_drinks_demand_offset
}
local_commoners_luxury_drinks_demand_base = {
	value = local_commoner_gdp_per_capita_display
	multiply = local_commoners_luxury_drinks_budget_share
	divide = local_commoners_luxury_drinks_P
	add = local_commoners_luxury_drinks_demand_offset
}
# tribesmen: no income term — hardcoded neutral
local_tribesmen_luxury_drinks_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: luxury_food
# ──────────────────────────────────────────────────────────────
local_nobles_luxury_food_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_luxury_food_budget_share
	divide = local_nobles_luxury_food_P
	add = local_nobles_luxury_food_demand_offset
}
local_clergy_luxury_food_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_luxury_food_budget_share
	divide = local_clergy_luxury_food_P
	add = local_clergy_luxury_food_demand_offset
}
local_burghers_luxury_food_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_luxury_food_budget_share
	divide = local_burghers_luxury_food_P
	add = local_burghers_luxury_food_demand_offset
}
local_commoners_luxury_food_demand_base = {
	value = local_commoner_gdp_per_capita_display
	multiply = local_commoners_luxury_food_budget_share
	divide = local_commoners_luxury_food_P
	add = local_commoners_luxury_food_demand_offset
}
# tribesmen: no income term — hardcoded neutral
local_tribesmen_luxury_food_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: luxury_goods
# ──────────────────────────────────────────────────────────────
local_nobles_luxury_goods_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_luxury_goods_budget_share
	divide = local_nobles_luxury_goods_P
	add = local_nobles_luxury_goods_demand_offset
}
local_clergy_luxury_goods_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_luxury_goods_budget_share
	divide = local_clergy_luxury_goods_P
	add = local_clergy_luxury_goods_demand_offset
}
local_burghers_luxury_goods_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_luxury_goods_budget_share
	divide = local_burghers_luxury_goods_P
	add = local_burghers_luxury_goods_demand_offset
}
# P_g_s = 0 ≈ 0 — hardcoded neutral
local_commoners_luxury_goods_demand_base = { value = 1 }
# tribesmen: no income term — hardcoded neutral
local_tribesmen_luxury_goods_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: protein
# ──────────────────────────────────────────────────────────────
local_nobles_protein_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_protein_budget_share
	divide = local_nobles_protein_P
	add = local_nobles_protein_demand_offset
}
local_clergy_protein_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_protein_budget_share
	divide = local_clergy_protein_P
	add = local_clergy_protein_demand_offset
}
local_burghers_protein_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_protein_budget_share
	divide = local_burghers_protein_P
	add = local_burghers_protein_demand_offset
}
local_commoners_protein_demand_base = {
	value = local_commoner_gdp_per_capita_display
	multiply = local_commoners_protein_budget_share
	divide = local_commoners_protein_P
	add = local_commoners_protein_demand_offset
}
# tribesmen: no income term — hardcoded neutral
local_tribesmen_protein_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: spices
# ──────────────────────────────────────────────────────────────
local_nobles_spices_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_spices_budget_share
	divide = local_nobles_spices_P
	add = local_nobles_spices_demand_offset
}
local_clergy_spices_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_spices_budget_share
	divide = local_clergy_spices_P
	add = local_clergy_spices_demand_offset
}
local_burghers_spices_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_spices_budget_share
	divide = local_burghers_spices_P
	add = local_burghers_spices_demand_offset
}
# P_g_s = 0 ≈ 0 — hardcoded neutral
local_commoners_spices_demand_base = { value = 1 }
# tribesmen: no income term — hardcoded neutral
local_tribesmen_spices_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: precious
# ──────────────────────────────────────────────────────────────
local_nobles_precious_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_precious_budget_share
	divide = local_nobles_precious_P
	add = local_nobles_precious_demand_offset
}
# P_g_s = 0 ≈ 0 — hardcoded neutral
local_clergy_precious_demand_base = { value = 1 }
local_burghers_precious_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_precious_budget_share
	divide = local_burghers_precious_P
	add = local_burghers_precious_demand_offset
}
# P_g_s = 0 ≈ 0 — hardcoded neutral
local_commoners_precious_demand_base = { value = 1 }
# tribesmen: no income term — hardcoded neutral
local_tribesmen_precious_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: treasures
# ──────────────────────────────────────────────────────────────
local_nobles_treasures_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_treasures_budget_share
	divide = local_nobles_treasures_P
	add = local_nobles_treasures_demand_offset
}
# P_g_s = 0 ≈ 0 — hardcoded neutral
local_clergy_treasures_demand_base = { value = 1 }
local_burghers_treasures_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_treasures_budget_share
	divide = local_burghers_treasures_P
	add = local_burghers_treasures_demand_offset
}
# P_g_s = 0 ≈ 0 — hardcoded neutral
local_commoners_treasures_demand_base = { value = 1 }
# tribesmen: no income term — hardcoded neutral
local_tribesmen_treasures_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: medicine
# ──────────────────────────────────────────────────────────────
local_nobles_medicine_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_medicine_budget_share
	divide = local_nobles_medicine_P
	add = local_nobles_medicine_demand_offset
}
local_clergy_medicine_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_medicine_budget_share
	divide = local_clergy_medicine_P
	add = local_clergy_medicine_demand_offset
}
local_burghers_medicine_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_medicine_budget_share
	divide = local_burghers_medicine_P
	add = local_burghers_medicine_demand_offset
}
local_commoners_medicine_demand_base = {
	value = local_commoner_gdp_per_capita_display
	multiply = local_commoners_medicine_budget_share
	divide = local_commoners_medicine_P
	add = local_commoners_medicine_demand_offset
}
# tribesmen: no income term — hardcoded neutral
local_tribesmen_medicine_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: ritual
# ──────────────────────────────────────────────────────────────
local_nobles_ritual_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_ritual_budget_share
	divide = local_nobles_ritual_P
	add = local_nobles_ritual_demand_offset
}
local_clergy_ritual_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_ritual_budget_share
	divide = local_clergy_ritual_P
	add = local_clergy_ritual_demand_offset
}
local_burghers_ritual_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_ritual_budget_share
	divide = local_burghers_ritual_P
	add = local_burghers_ritual_demand_offset
}
local_commoners_ritual_demand_base = {
	value = local_commoner_gdp_per_capita_display
	multiply = local_commoners_ritual_budget_share
	divide = local_commoners_ritual_P
	add = local_commoners_ritual_demand_offset
}
# tribesmen: no income term — hardcoded neutral
local_tribesmen_ritual_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: weapons
# ──────────────────────────────────────────────────────────────
local_nobles_weapons_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_weapons_budget_share
	divide = local_nobles_weapons_P
	add = local_nobles_weapons_demand_offset
}
# P_g_s = 0 ≈ 0 — hardcoded neutral
local_clergy_weapons_demand_base = { value = 1 }
# P_g_s = 0 ≈ 0 — hardcoded neutral
local_burghers_weapons_demand_base = { value = 1 }
local_commoners_weapons_demand_base = {
	value = local_commoner_gdp_per_capita_display
	multiply = local_commoners_weapons_budget_share
	divide = local_commoners_weapons_P
	add = local_commoners_weapons_demand_offset
}
# tribesmen: no income term — hardcoded neutral
local_tribesmen_weapons_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: mounts
# ──────────────────────────────────────────────────────────────
local_nobles_mounts_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_mounts_budget_share
	divide = local_nobles_mounts_P
	add = local_nobles_mounts_demand_offset
}
# P_g_s = 0 ≈ 0 — hardcoded neutral
local_clergy_mounts_demand_base = { value = 1 }
# P_g_s = 0 ≈ 0 — hardcoded neutral
local_burghers_mounts_demand_base = { value = 1 }
# P_g_s = 0 ≈ 0 — hardcoded neutral
local_commoners_mounts_demand_base = { value = 1 }
# tribesmen: no income term — hardcoded neutral
local_tribesmen_mounts_demand_base = { value = 1 }

# ──────────────────────────────────────────────────────────────
#  GROUP: knowledge
# ──────────────────────────────────────────────────────────────
local_nobles_knowledge_demand_base = {
	value = local_noble_gdp_per_capita_display
	multiply = local_nobles_knowledge_budget_share
	divide = local_nobles_knowledge_P
	add = local_nobles_knowledge_demand_offset
}
local_clergy_knowledge_demand_base = {
	value = local_clergy_gdp_per_capita_display
	multiply = local_clergy_knowledge_budget_share
	divide = local_clergy_knowledge_P
	add = local_clergy_knowledge_demand_offset
}
local_burghers_knowledge_demand_base = {
	value = local_burghers_gdp_per_capita_display
	multiply = local_burghers_knowledge_budget_share
	divide = local_burghers_knowledge_P
	add = local_burghers_knowledge_demand_offset
}
# P_g_s = 0 ≈ 0 — hardcoded neutral
local_commoners_knowledge_demand_base = { value = 1 }
# tribesmen: no income term — hardcoded neutral
local_tribesmen_knowledge_demand_base = { value = 1 }

