# Pop-scope demand scale routing wrappers.
# Each value delegates to the corresponding location-scope value in
# z_SOL_group_demand_scales_location.txt based on pop_type.
# Canonical calculations live there (single source of truth).

sol_staple_demand_scale = {
	value = 0
	if = {
		limit = { pop_type = pop_type:nobles }
		add = location.local_nobles_staple_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:clergy }
		add = location.local_clergy_staple_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:burghers }
		add = location.local_burghers_staple_demand_scale
	}
	else_if = {
		limit = {
			OR = {
				pop_type = pop_type:laborers
				pop_type = pop_type:soldiers
				pop_type = pop_type:peasants
			}
		}
		add = location.local_commoners_staple_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:tribesmen }
		add = location.local_tribesmen_staple_demand_scale
	}
}

sol_protein_demand_scale = {
	value = 0
	if = {
		limit = { pop_type = pop_type:nobles }
		add = location.local_nobles_protein_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:clergy }
		add = location.local_clergy_protein_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:burghers }
		add = location.local_burghers_protein_demand_scale
	}
	else_if = {
		limit = {
			OR = {
				pop_type = pop_type:laborers
				pop_type = pop_type:soldiers
				pop_type = pop_type:peasants
			}
		}
		add = location.local_commoners_protein_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:tribesmen }
		add = location.local_tribesmen_protein_demand_scale
	}
}

sol_alcohol_demand_scale = {
	value = 0
	if = {
		limit = { pop_type = pop_type:nobles }
		add = location.local_nobles_alcohol_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:clergy }
		add = location.local_clergy_alcohol_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:burghers }
		add = location.local_burghers_alcohol_demand_scale
	}
	else_if = {
		limit = {
			OR = {
				pop_type = pop_type:laborers
				pop_type = pop_type:soldiers
				pop_type = pop_type:peasants
			}
		}
		add = location.local_commoners_alcohol_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:tribesmen }
		add = location.local_tribesmen_alcohol_demand_scale
	}
}

sol_military_demand_scale = {
	value = 0
	if = {
		limit = { pop_type = pop_type:nobles }
		add = location.local_nobles_military_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:clergy }
		add = location.local_clergy_military_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:burghers }
		add = location.local_burghers_military_demand_scale
	}
	else_if = {
		limit = {
			OR = {
				pop_type = pop_type:laborers
				pop_type = pop_type:soldiers
				pop_type = pop_type:peasants
			}
		}
		add = location.local_commoners_military_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:tribesmen }
		add = location.local_tribesmen_military_demand_scale
	}
}

sol_spices_demand_scale = {
	value = 0
	if = {
		limit = { pop_type = pop_type:nobles }
		add = location.local_nobles_spices_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:clergy }
		add = location.local_clergy_spices_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:burghers }
		add = location.local_burghers_spices_demand_scale
	}
	else_if = {
		limit = {
			OR = {
				pop_type = pop_type:laborers
				pop_type = pop_type:soldiers
				pop_type = pop_type:peasants
			}
		}
		add = location.local_commoners_spices_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:tribesmen }
		add = location.local_tribesmen_spices_demand_scale
	}
}

sol_stimulants_demand_scale = {
	value = 0
	if = {
		limit = { pop_type = pop_type:nobles }
		add = location.local_nobles_stimulants_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:clergy }
		add = location.local_clergy_stimulants_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:burghers }
		add = location.local_burghers_stimulants_demand_scale
	}
	else_if = {
		limit = {
			OR = {
				pop_type = pop_type:laborers
				pop_type = pop_type:soldiers
				pop_type = pop_type:peasants
			}
		}
		add = location.local_commoners_stimulants_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:tribesmen }
		add = location.local_tribesmen_stimulants_demand_scale
	}
}

sol_textiles_demand_scale = {
	value = 0
	if = {
		limit = { pop_type = pop_type:nobles }
		add = location.local_nobles_textiles_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:clergy }
		add = location.local_clergy_textiles_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:burghers }
		add = location.local_burghers_textiles_demand_scale
	}
	else_if = {
		limit = {
			OR = {
				pop_type = pop_type:laborers
				pop_type = pop_type:soldiers
				pop_type = pop_type:peasants
			}
		}
		add = location.local_commoners_textiles_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:tribesmen }
		add = location.local_tribesmen_textiles_demand_scale
	}
}

sol_knowledge_demand_scale = {
	value = 0
	if = {
		limit = { pop_type = pop_type:nobles }
		add = location.local_nobles_knowledge_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:clergy }
		add = location.local_clergy_knowledge_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:burghers }
		add = location.local_burghers_knowledge_demand_scale
	}
	else_if = {
		limit = {
			OR = {
				pop_type = pop_type:laborers
				pop_type = pop_type:soldiers
				pop_type = pop_type:peasants
			}
		}
		add = location.local_commoners_knowledge_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:tribesmen }
		add = location.local_tribesmen_knowledge_demand_scale
	}
}

sol_ritual_demand_scale = {
	value = 0
	if = {
		limit = { pop_type = pop_type:nobles }
		add = location.local_nobles_ritual_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:clergy }
		add = location.local_clergy_ritual_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:burghers }
		add = location.local_burghers_ritual_demand_scale
	}
	else_if = {
		limit = {
			OR = {
				pop_type = pop_type:laborers
				pop_type = pop_type:soldiers
				pop_type = pop_type:peasants
			}
		}
		add = location.local_commoners_ritual_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:tribesmen }
		add = location.local_tribesmen_ritual_demand_scale
	}
}

sol_precious_demand_scale = {
	value = 0
	if = {
		limit = { pop_type = pop_type:nobles }
		add = location.local_nobles_precious_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:clergy }
		add = location.local_clergy_precious_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:burghers }
		add = location.local_burghers_precious_demand_scale
	}
	else_if = {
		limit = {
			OR = {
				pop_type = pop_type:laborers
				pop_type = pop_type:soldiers
				pop_type = pop_type:peasants
			}
		}
		add = location.local_commoners_precious_demand_scale
	}
	else_if = {
		limit = { pop_type = pop_type:tribesmen }
		add = location.local_tribesmen_precious_demand_scale
	}
}
