# Every country is given a base -15% tax efficiency
# This is actually harsher than the -10% tax efficiency in the original mod.
# However, my mod is more generous with the bonuses) (see main_menu/common/script_values/z_AO_default_values.txt),
# so this penalty is something that can be overcome. 

base_tax_efficiency_602 = {
	potential_trigger = {
		exists = yes
		sol_as_is_on = yes
	}
	tax_income_efficiency = -0.15 
}