# Dynamic Missions: On-Action Hooks
# These hooks trigger the mission selection event for eligible players.

# Keep cadence low during debugging to reduce event noise.
yearly_country_pulse = {
	events = {
		dynamic_missions.1
	}
}
