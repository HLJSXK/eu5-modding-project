# AI Tool Workflow Prompt (EU5)

Use the following prompt for AI coding tools in this project:

```text
You are an expert Europa Universalis 5 (EU5) modder. EU5 uses an updated Jomini engine. Do not assume EU4 syntax works.

### Workflow: The 3-Step Resolution Rule
When proposing code edits or generating new scripts, you must evaluate your knowledge and follow this exact sequence:

1. **Direct Edit**: If you are 100% certain about the EU5 syntax (e.g., standard Jomini logic), write the script directly.
2. **Consult Docs**: If you are unsure about a specific `script_value`, `data_type`, trigger, or effect, you MUST read the reference files in the `reference_official_defines/` workspace folder first.
3. **Consult Source Files**: If the answer is not in `reference_official_defines/`, search the `reference_game_files/` and `reference_mods/` workspace folder for real-world implementations before writing the code.

### Mandatory Reference Categories (Step 1 is FORBIDDEN)

For the following syntax categories, you are **never** allowed to rely on memory or inference alone.
You MUST go directly to Step 2 or Step 3 before writing or modifying any code:

| Category | Why Step 1 is forbidden |
|---|---|
| `blockoverride` block names and their allowed child properties | Block names are template-specific; accepted property types differ per block |
| `custom_tooltip` key formats (e.g. dotted suffixes like `.tooltip`) | Suffix rules are not documented; guessing caused a feature to be incorrectly removed |
| `situation_card_common` / `card_common` template structure | Inner block rules are invisible without reading `cards.gui` |
| `location_rank:*` enum values | Enum values are engine-defined; wrong values (e.g. `village`) produce silent failures |
| Any `static_modifier`, `country_modifier`, `location_modifier` name | Modifier names must exist in defines; typos cause silent no-ops |
| Any `scripted_trigger` or `scripted_effect` not defined in this mod | Vanilla names change between patches |
| Localization key format rules (YAML encoding, quote characters) | PDX parser rejects non-ASCII quotes; encoding errors cascade silently |
| GUI expression syntax (`GetVariable`, `.IsSet`, `MakeScope`, etc.) | Expression language is undocumented; wrong patterns produce no visible error |
| Any GUI icon display | Must check `font_icons.gui` for `@xxx!` inline texticon before using widget or custom approach |

### Declarative Verification Requirement

Before writing or modifying any code that falls under the Mandatory Reference Categories above,
you MUST output a verification line in this exact format:

> **Verification** — Step [2/3], Reference: `[file path]:[line number]`, Quote: `"[exact text from source]"`

This line must appear **before** the code block. No code may be written without it.

If you cannot locate a suitable reference, output:

> **Verification** — FAILED. Cannot verify `[syntax in question]`. Reporting to user before proceeding.

Then stop and ask the user for guidance. Do NOT guess.

### Constraints
- NEVER hallucinate or guess Paradox script syntax.
- If you cannot verify a command using the steps above, explicitly tell the user: "I cannot verify this syntax, please check the official wiki or logs."
- If a syntax pattern causes bugs, do NOT remove the feature as a first response. You MUST follow the 3-step rule in order (Direct Edit -> reference_official_defines/ -> reference_game_files/) and replace it with a verified working syntax.
```

## Path Mapping In This Repository

- `docs/` -> project docs and technical notes
- `reference_official_defines/` -> official define/type reference files
- `reference_game_files/` -> vanilla script source files
- `reference_mods/` -> some representative community mods

## Required Behavior For Bug Fixing

- When a previously implemented script/GUI expression fails, the default action is **syntax replacement based on verification**, not feature removal.
- Removal or fallback simplification is only allowed when:
  - the syntax cannot be verified in `reference_official_defines/`, `reference_game_files/` and `reference_mods/`, and
  - the tool explicitly reports this uncertainty to the user.

## GUI Icon Display Priority

When displaying an icon in any UI context, AI tools MUST follow this priority and stop at the first applicable tier:

| Tier | Method | When to use |
|---|---|---|
| 1 | `@icon_name!` inline syntax | Icon exists in `font_icons.gui`; context is `raw_text`/`text`/localization YAML |
| 2 | Icon widget (`icon = { texture = "..." }`) | Standalone widget needed, or icon absent from `font_icons.gui` |
| 3 | From scratch (new `texticon` / new sprite) | Tiers 1 and 2 both inapplicable; must justify explicitly |

Authoritative icon list: `reference_game_files/game/main_menu/gui/shared/font_icons.gui` (180+ entries)

## Documented Violations (Learning Record)

The following violations occurred and informed the Mandatory Reference Categories above:

| Date | Violation | Root cause | Correct behavior |
|---|---|---|---|
| 2026-08 | Initially searched country setup and government configuration for a command that enables the Dhimmi estate | Treated an estate as a country-level feature flag and conflated starting privileges with estate creation | For population-backed conditional estates, read `common/pop_types` first and follow the mapping trigger. Vanilla regular pops use `dhimmi_estate = { is_dhimmi = yes }`; the pop-scoped trigger requires a protected non-Muslim religion group under a Muslim owner. Muslim setup templates only select starting Dhimmi privileges. |
| 2026-08 | Passed `{ value/divide/min }` calculation blocks directly to `add_to_global_variable_map.value`, producing event-target and unreadable-field errors in SOL CMF diagnostics | Assumed the map effect accepted the same inline arithmetic syntax as `set_variable` | Compute the value with `set_variable`, pass `value = var:<temporary>` to the map write, then remove the temporary variable. Map values accept a direct scalar/event target, not a script-math block. |
| 2026-08 | Used `max = 0` to clamp the exact-derived minimax factors to nonnegative values, which silently zeroed positive factors at runtime | Read `max` as a lower-bound operator instead of the upper clamp used by EU5 value expressions | Use `min = 0` for a nonnegative lower bound and reserve `max` for an actual upper limit. The HUN runtime diagnostic showed `0 / 0 / 0 / 1.16986` where offline exact factors were positive. |
| 2026-08 | Used `ordered_owned_location` without `max` for SOL's full classification pass, so 189 scored locations produced only one assigned location | Assumed omitting `max` meant unlimited ordered traversal; at runtime the ordered scope returned its default single result | Every `ordered_*` loop intended to process all matches must set an explicit sufficient `max` (or a verified dynamic bound). Use the default or `max = 1` only for deliberate best-candidate selection. |
| 2026-08 | Packed SOL class profiles and matrix equations into oversized CMF log entries | Optimized for maximum diagnostic density without accounting for the log pane's single-row readability | Keep each CMF entry to one concept and at most four dynamic values. Use compact labels; split only essential wide vectors and omit secondary fields instead of creating unreadable rows. |
| 2026-07 | Generated SOL's yearly per-market refresh with `set_local_variable` / `change_local_variable` scratch values but no cleanup | Treated Jomini local variables as automatically discarded on effect return | After the last read, remove every temporary local variable at its matching evaluation scope. Keep declarations and removals in the same generator so base, PP, M&T, and JTG targets stay covered. |
| 2026-07 | Calibrated SOL strata income and pop demand while retaining vanilla `NPop.POP_NEEDS_INCOME_SCALE = 1.33` | Treated the define as a harmless internal scale instead of a purchase-side expenditure surcharge | Set `NPop.POP_NEEDS_INCOME_SCALE = 1` in stable and standalone. Runtime testing shows `1.33` makes pops pay 33% above the nominal goods cost, independently of demand quantities and development controls. |
| 2026-07 | Repeated the full SOL scripted-effect file under a `zz_` name to provide PP-aware branches | Assumed a later plain definition replaced an existing scripted-effect key | Output only effects that differ and prefix each with `REPLACE:`. The engine otherwise logs `Duplicated key ... will not be created` and keeps the original effect. |
| 2026-07 | Used `TRY_INJECT` to append another `allow` block to `reduce_war_exhaustion` | Assumed database injection deep-merged nested trigger sections | Verify and copy the complete action, then use `REPLACE:` for nested-logic changes; duplicate singleton trigger sections produce `Trigger section already read earlier`. |
| 2026-07 | Kept a disabled feature's CMF/CMM setting conceptually tied to old-save cleanup instead of treating registration as the UI source of truth | Did not verify whether CMM rebuilt group membership or persisted the old setting list forever | Replace the mod registration effect and omit disabled setting IDs and callbacks. CMM clears each group's setting list on registration, removing stale old-save UI entries; hard-disable the gameplay trigger separately. |
| 2026-07 | Considered reusing a legacy CMM setting ID with `default_value = 0` for a new compatibility opt-in | Overlooked that CMM applies defaults only when the setting's variable-map key is missing, so upgraded saves can retain the old enabled value | Use a compatibility-specific setting ID, omit the legacy ID, sync the new default-off setting to the existing gameplay alias, and keep the trigger false when CMM is absent. Do not rewrite CMM internal maps. |
| 2026-07 | Used `building_category != government_category` in the SOL-M&T EPBM cache injection | Treated a simple-assign trigger like a comparison expression | Simple-assign triggers only accept `=`. Express inequality as `NOT = { building_category = government_category }`; otherwise the scripted effect fails to compile. |
| 2026-07 | Removed SOL economic CMM settings in the M&T compatibility registration but left their script-value and scripted-GUI variable readers active | Treated removing the registered controls as sufficient cleanup and overlooked the engine's reachability-based variable analysis | Exact-path override the residual CMM files: preserve variable-free fallback script values and only callbacks for settings still registered. Setters in unused scripted objects do not satisfy the engine. |
| 2026-07 | Called the pop-scoped `is_gaelic_clans` trigger directly from country-scoped EPBM finalization | Focused on its nested `owner` privilege test and overlooked the trigger's pop entry scope | Verify the complete scripted-trigger body and its entry scope. In country scope, test the two Gaelic estate privileges directly instead of invoking the pop trigger. |
| 2026-07 | Used a late negative `TRY_INJECT` to cancel SOL road-price fields in the PP compatibility submod | Assumed repeated price resource fields accumulated like numeric static modifiers | Repeated price scalars replace earlier values. Let PP's later same-operation filename own the road prices; never inject a negative price cleanup, which becomes authoritative and can clamp the runtime cost to zero. Static-modifier numeric entries remain additive and must be handled separately. |
| 2026-07 | Calibrated SOL pop demand while development still affected the result through both a global multiplier and per-good gates | Treated development influence as one mechanism; EU5 actually uses `NPop.DEVELOPMENT_SCALE_ON_DEMAND` plus separate `development_threshold` fields on goods | Disable both layers: set `NPop.DEVELOPMENT_SCALE_ON_DEMAND = 0`, fully negate every vanilla goods threshold with `INJECT`, and separately negate thresholds introduced by compatibility mods such as PP `victuals` |
| 2026-07 | Treated a later filename or `zz_` prefix as a universal way for a compatibility patch to win an `INJECT`/`REPLACE` conflict | Conflated exact-path file overwrite, ASCII file loading, and database operation processing into one load-order rule | Separate exact-path collisions from object edits. For object edits, apply operation priority first (`INJECT_OR_CREATE` -> `REPLACE_OR_CREATE` -> `TRY_INJECT` -> `TRY_REPLACE` -> `INJECT` -> `REPLACE`), and compare filenames only when both entries use the same operation type. |
| 2026-07 | Kept SOL's final `location_window.gui` based on vanilla, then initially treated a Glorp-based Built-in window as requiring Glorp UI and Construction Manager at runtime | Looked only at the whole-file merge base and did not distinguish source provenance from runtime ownership | Audit identical paths and every external reference. A Built-in copy must vendor required support types under SOL-owned names and remove CM-only widget/script/mapmode calls; source metadata may be checked for drift without becoming a mod dependency. |
| 2026-06 | Computed SOL country estate income/expense ratios during the first game month | EU5 does not include estate trade income until after the first game month, so the first-month ratio understates income and biases poverty compensation | Keep the compensation factor neutral before `current_date >= 1337.5.1`; initialize it once from `monthly_country_pulse` in the second month before applying `local_pop_demand`, while allowing old saves to refresh immediately |
| 2026-06 | Copied vanilla/CMF entries into `monthly_country_pulse` to preserve existing pulse dispatchers | Assumed duplicate pulse definitions replaced each other and that all on_actions had to be manually merged | Use the compact `yearly_country_pulse` style for both monthly and yearly country pulses: define only this mod's `on_actions` entries. The engine merges `on_actions` from duplicate pulse definitions automatically. |
| 2026-03 | Removed `custom_tooltip` from event options | Guessed dotted key format was invalid; skipped Steps 2/3 | Read `reference_game_files/`; `ali_qushji_settles.tooltip` confirms dotted keys are valid |
| 2026-03 | Used `location_rank:village` | Guessed enum value; did not check defines | Read `reference_official_defines/`; valid values are `rural_settlement`, `town`, `city` |
| 2026-03 | Placed child `text_single` inside `blockoverride "common_header_text"` | Guessed block accepted child widgets; skipped reading `cards.gui` | Read `cards.gui:1084`; block overrides a `text` property, not a widget container |
| 2026-04 | Used `value = location.local_*` inside a location-scope script value | Assumed `location.` prefix was always required for location variables | `location.` is a navigation link from another scope; inside a location-scope value, reference variables directly without the prefix |
| 2026-04 | Generated script_values with 6-decimal float literals (e.g. `0.084771`) | No awareness of EU5 engine's 5dp precision limit | Round all float literals to ≤5 decimal places in generated and hand-written mod files; engine silently truncates anything beyond the 5th digit |
| 2026-04 | Proposed `datamodel = "[GoodsView.GetGoods]"` inside ContextualTooltipType to filter by key and obtain a Goods datacontext | Assumed GoodsView is a globally accessible object; it is panel-scoped only | No all-goods datamodel exists in tooltip scope; GoodsView is only in goods_overview.gui; no GetGoods('key') string lookup exists anywhere in EU5 GUI |
| 2026-05 | Proposed `multiply_global_variable = { name = X value = 0.95 }` to scale a global variable in-place | Assumed EU5 has a multiply variant analogous to `change_global_variable` | No `multiply_global_variable` effect exists; use hardcoded `set_global_variable` values per case (idempotent) or compute in a local_variable first |
| 2026-05 | Wrote `multiply = global_var:sol_era_coeff` directly in script_values; engine validation pass ran before any on_action and emitted "Failed to fetch variable" / "Got value of type none" errors even with `on_game_start` init wired up | Assumed `on_game_start` was the earliest hook; in fact EU5 evaluates script_values during file-load validation BEFORE any on_action fires | Wrap unguarded global reads: `local_NAME = { value = <default>  if = { limit = { has_global_variable = NAME }  value = global_var:NAME } }`, then `multiply = local_NAME`. on_game_start init is still useful as defense-in-depth but cannot prevent the load-time error alone |
| 2026-05 | Codegen emitted `change_variable = { name = X  value = Y }`; engine logged "change_variable effect [ No operation specified ]" and silently dropped every delta | Mirrored `set_variable`'s `value =` syntax onto `change_variable` without adapting; assumed `value` was a generic write keyword | Use `change_variable = { name = X  add = Y }` (or subtract/multiply/divide). `value =` is reserved for `set_variable` only; `change_variable` requires an operation keyword |
| 2026-05 | Wrote `value = variable:sol_market_scarcity_adj_nobles` in a script_value; engine logged "Failed to find a valid event target link 'variable:NAME'" | Mistook the long form `variable:` (which appears in informal comments) for the actual event-target prefix | EU5's event-target prefix for scope variables is `var:` — short form only. `variable` is only a keyword inside `set_variable` / `change_variable` / `has_variable` blocks (as a field name), never as a `:` -prefixed reference |
| 2026-05 | Cached SOL per-stratum scarcity adjustments as location variables, computed by iterating `every_owned_location` and calling 5×`SOL_compute_scarcity_score_<strata>` per location every January 1; produced ~6.7M conditional checks per Jan-1 frame and reported in-game lag spike | Did not recognise that the 336 checks per stratum effect were all market-keyed (`market ?= { is_target_in_global_variable_list ... }`) and therefore identical for every owned location sharing a market | Cache market-keyed data once per market. For EU5 1.3 SOL, use `global_variable_map` with the market scope as key rather than storing synthetic variables on the market center location. Markets do NOT support direct `set_variable`, but they are valid global map keys. |
| 2026-06 | Stored SOL market consumed-goods flags on `market.location` and read them in GUI via `Location.GetMarket.GetCenterLocation.MakeScope.GetVariable(...)` | Treated the market center location as a convenient per-market cache owner even though the data is keyed by market identity | Use `global_variable_map` for market-keyed SOL caches. Script writes with `remove_from_global_variable_map` followed by `add_to_global_variable_map`, using `key = scope:sol_market_cache`; GUI reads with `GetVariableFromGlobalVariableMap('sol_market_*', Location.GetMarket.MakeScope)`. `GetCenterLocation` is only for cases that truly need the center Location object. |
| 2026-06 | Used `demands_goods_by_pops = goods:<good>` in SOL yearly market maps to decide whether POPs consume or demand the good in a market | Misread a shortage trigger as a generic market-demand/consumption trigger; its localization describes pop demand exceeding supply | For a known market, scope to the good and call `is_demanded_in_market_by_pops = scope:<market>`, e.g. `goods:wine = { is_demanded_in_market_by_pops = scope:sol_market_cache }`. Reserve `demands_goods_by_pops` for shortage checks. |
| 2026-06 | Used `add_to_global_variable_map` alone to refresh an existing key | Assumed `add_to_*_variable_map` overwrites values like `set_variable`; the engine silently keeps the old value when the key already exists | Treat map writes as insert-only: first `remove_from_global_variable_map = { name = X key = scope:Y }`, then `add_to_global_variable_map = { name = X key = scope:Y value = Z }`. |
| 2026-06 | Used country `set_variable` flags only to feed the SOL situation GUI, producing `Variable X is set but is never used` warnings from `jomini_effect.cpp:1162` | Assumed GUI reads like `Player.MakeScope.GetVariable(...)` counted as script-side variable use; Jomini's unused-variable pass does not count GUI/localization-only reads | Ensure display caches are read in a live script path. Use `global_variable_map` only when keyed storage is appropriate; GUI reads alone still do not satisfy unused-variable validation. |
| 2026-06 | Wrote SOL country consumed-goods flags as `gls_country_consumes_*` global_variable_map entries and read them only through `GetVariableFromGlobalVariableMap(...)` in the situation GUI; engine still logged `Variable X is set but is never used` | Assumed either GUI map reads counted, or using `global_variable_map` was enough to avoid unused-variable validation | Keep `global_variable_map` for keyed display caches when appropriate, but add a live script-side read of each map name using `global_variable_map(name|scope:key)` inside an effect/trigger that actually runs. A local-variable count or no-op read is sufficient when the value is display-only. |
| 2026-06 | Tried to force a situation to use a separate custom mapmode from the situation script definition | Assumed situation `on_start` or a `map_mode = <tag>` field could switch the player's mapmode | Situation script definitions expose data-map coloring but no verified custom-mapmode field. Put a zero-size widget in the situation-specific panel and use `trigger_on_create` to call `[PdxGuiWidget.PushMapModeOverride('<tag>')]`; destruction of that widget removes its mapmode override. |
| 2026-06 | Put `datacontext = "[GetMapMode('<tag>')]"` on a hidden widget and called `[MapMode.SetMapMode]` from `state.on_start`; the panel loaded but the mapmode did not switch | Assumed `state.on_start` inherited the widget's MapMode datacontext for method dispatch | `state.on_start` does not inherit that datacontext; error.log reports "No context supplied ... wanted context of type 'MapMode'". Call the object-returning expression directly: `[GetMapMode('<tag>').SetMapMode]`. |
| 2026-07 | Used `_show`/`_hide` states on a zero-size SOL situation child to enter and leave its mapmode; opening the panel did not switch mapmode | Assumed `_show` was also a construction callback; the situation child is instantiated after its parent is already visible, so no hidden-to-shown transition occurs | Use `trigger_on_create` with `[PdxGuiWidget.PushMapModeOverride('sol_living_standard')]`. Unlike global `SetMapMode`/`SetDefaultMapMode`, the widget-owned override starts on construction and is removed with the situation-specific widget. |
| 2026-06 | Tried to reason about a monthly location modifier icon as if it belonged in `add_location_modifier` or the static modifier definition | Missed that modifier rows and static modifier tooltips resolve icons through `main_menu/common/modifier_icons` data | Put icon mappings in `main_menu/common/modifier_icons/<file>.txt`, using `positive`/`negative` texture paths. For custom static modifiers, map the static modifier id; only map the contained modifier type if that key is not already defined by vanilla or another loaded mod. |
| 2026-06 | Added `local_pop_demand = { ... }` to a mod `modifier_icons` file to override the icon, causing `Duplicated key local_pop_demand will not be created` | Forgot that `modifier_icons` is a keyed database and `local_pop_demand` already exists in vanilla `00_modifier_icons.txt` | Do not duplicate vanilla modifier icon keys in additive files. For SOL's monthly location modifier, map `sol_local_pop_demand_modifier` to the DDS and leave vanilla `local_pop_demand` alone unless using an explicit replacement strategy. |
| 2026-06 | Generated a custom mapmode DDS with only 7 mip levels for a 128x128 texture and `dwDepth = 0`; direct GUI texture references could still load it, but `MapMode.GetIcon` did not use the resource | Assumed any valid DXT5 DDS accepted by direct GUI texture widgets would also be accepted by the mapmode icon resolver | Match reference mapmode DDS exports: keep icons at `main_menu/gfx/interface/icons/map_modes/<map_mode_id>.dds`, include a full mip chain down to 1x1, and write a Clausewitz-style 2D header with `dwDepth = 1`. |
| 2026-06 | Added `pinned_by_default = yes` to `in_game/gfx/map/map_modes/SOL_map_modes.txt`, causing `Malformed token: yes` during mapmode file load | Treated `MapMode.IsMapModePinnedByDefault` as proof that `pinned_by_default` is a valid writable mapmode data field | Do not write `pinned_by_default` in mapmode data unless a current official schema verifies it. Keep `category` and `index` so the mapmode remains selectable; default pinning is not worth breaking parser load. |
| 2026-06 | Counted estate buildings conceptually as normal location buildings or hard-coded type lists | Missed that building scope exposes `estate_type`, so the count should be filtered by the building's estate type rather than by names or raw `num_buildings` | Iterate `every_buildings_in_location` from a location and filter with `estate_type ?= estate_type:<estate>`, e.g. `estate_type ?= estate_type:nobles_estate`. |
| 2026-05 | Used block syntax `wine = { value = "SOL_wine_demand" }` in pop_demand good entries; engine loaded only the first good and dropped all subsequent entries plus `category = pop_needs` | Assumed `{ value = ... }` block wrapper was valid for script value references in this context; it had worked in an earlier engine version | pop_demand good entries require plain direct assignment matching the vanilla pattern: `wine = SOL_wine_demand`. Both quoted strings and `{ value = }` block wrappers are broken — only `good = script_value` (no block, no quotes) is accepted |
| 2026-05 | anti_patterns regex `change_variable\s*=\s*\{[^}]*\bvalue\s*=` flagged the legal nested form `change_variable = { name = X  add = { value = Y  multiply = Z } }` as invalid (~73 false positives in `gls_accumulate_panel_stats`) | Used `[^}]*` which doesn't consume `}` but freely crosses `{`, so the scanner reached the inner `value = Y` of the nested `add`-with-operations block | Use `[^{}]*` instead — exclude both `{` and `}` from the span so the regex stops at any brace boundary and only matches top-level `value = ` keys inside the change_variable block |
| 2026-05 | EU5 1.2 emitted `Event #sol_ae.X is missing an outcome in events/SOL_economy_events.txt` for all 7 SOL events on game-load | 1.2 added a new validator (`event_database.cpp:648`) that requires every event block to declare a top-level `outcome = ...` field; events written under 1.1 did not need it | Add `outcome = neutral` (or `good` / `bad` / `positive` / `negative` to match flavor) under `desc = ...` in every event. Vanilla `ages_of_eu.1` at `reference_game_files/game/in_game/events/ages.txt:7` uses `outcome = neutral` for tooltip-only notification events, which fits the SOL `sol_ae.*` and `sol_migration.*` style |
| 2026-06 | Treated the EU5 1.3 `local_pop_demand` migration as a reason to remove the SOL `demand_add` baseline | Confused the monthly runtime multiplier with the calibrated baseline data; missed that project multipliers are calibrated against corrected `demand_add` from `data/target_demand.csv` | Keep `data/target_demand.csv -> src/stable/in_game/common/goods/z_SOL_pop_goods.txt` as the protected calibrated `demand_add` baseline. Apply `local_pop_demand` only as the monthly location-level multiplier, and remove only the obsolete pop_demands/substitute/scarcity/Engel runtime chain |
| 2026-07 | Parsed the goods demand group `upper` as nobles, clergy, and burghers only when generating SOL `demand_add` values for EU5 1.3.11 | Carried forward the pre-1.3.11 group membership, understating vanilla laborer and soldier demand before calculating SOL injections | In 1.3.11, expand `upper` to nobles, clergy, burghers, laborers, and soldiers for both `demand_add` and `demand_multiply`, then regenerate every derived SOL demand file from `data/target_demand.csv` |
| 2026-08 | Registered the default-OFF `sol__ai_reduced_freq` CMM setting but initialized `sol_ai_reduced_freq_enabled` with `value = no`, called `cmm_add_scripted_gui` without defining `sol__ai_reduced_freq_on_changed`, and gated the on_action with a raw `has_global_variable` instead of a `sol_*_is_on` scripted trigger | Assumed a bool alias stores yes/no and only skimmed the sibling `cmm_register_*` calls instead of reading `cmm_sync_bool_alias` itself and auditing the full four-file registration chain used by every existing SOL setting | Read `reference_mods/3692202776/.../cmm_core_alias_effects.txt` first: `cmm_sync_bool_alias` calls `remove_global_variable` for OFF, so a default-OFF alias must be absent from the init effect. Register in exactly four places (register / sync / callback switch / `<mod_id>__<setting_id>_on_changed` scripted GUI), add a matching `sol_*_is_on` trigger plus `trigger_localization` and TT loc key, and invert the CMM-absent fallback so a default-OFF toggle reads false without CMM |
| 2026-08 | Explained a class-wide `-100%` `local_pop_demand` as "the approximation silently clamped a tiny negative coefficient to zero", then as "the matrix is ill-conditioned", then as "the yearly unit-spending refresh flipped it" — each rejected by the next measurement | Reasoned forward from plausible mechanisms instead of first computing the feasibility criterion; also treated linear independence / condition number as the criterion when the actual criterion is cone containment | Run `tools/eu5_save_parser/cone_feasibility.py` before any narrative. Exact-solve feasibility is `target ∈ cone(class columns)`, and `cone(any 4-way partition) ⊆ cone(all owned locations)`, so an all-location residual above ~0.01 closes off every classifier-side fix. A zero coefficient from the nonnegative L2 path is a legitimate boundary optimum, so tightening the gate to reject `delta < 0` cannot stop `= 0`. See `docs/technical/SOL_Country_Level_Strata_Demand_Model.md` section 3.9 |
| 2026-08 | Inferred SOL market unit-spending changes from basket composition and the save's per-good `supply` field, producing recomputations 1.3–2.6× above the engine's stored values, and blamed seasonal grain prices | Used the wrong supply filter: `goods_supply_in_market()` does not equal the good block's `supply` field. The generator already caches the engine's own answer in `sol_market_consumes_<good>` global maps, which was never read | Read the engine's own filter from the `sol_market_consumes_<good>` maps keyed by market scope. Recomputing with that set reproduces all four strata within 1.0025–1.0373 (residual is just price drift between refresh date and save date). Never infer a market-side quantity that the mod already persists |
| 2026-08 | Implemented the `sol__ai_reduced_freq` toggle by moving both the country solve and the `add_location_modifier` call to `yearly_country_pulse` with `years = 1`, letting the modifier's own lifetime carry the value between yearly solves | Reasoned only about the steady state, where consecutive yearly modifiers abut cleanly, and never enumerated the other entry points that write this modifier — `gls_full_refresh_country` (every game start and save load) applies the monthly `months = 1` form unconditionally | Split solve from apply: `sol_apply_local_pop_demand_modifiers` writes `months = 1` from the cached per-location size every month for every country, and the yearly pulse owns only the solve. Otherwise game start, save load, player-to-AI handover, a newly formed country, and flipping the toggle mid-game each leave an AI country on the bare `demand_add` baseline for up to 11 months, silently and with no log line |
